---
phase: 07-frontend-source-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/vite.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/context.tsx
  - frontend/src/Drawer.tsx
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/pages/DriftPage.tsx
  - frontend/src/pages/QuickTasksPage.tsx
  - frontend/src/pages/VerificationPage.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07
  - PERF-03
  - PERF-04

must_haves:
  truths:
    - "tsc -b completes with zero errors against the reconstructed source tree"
    - "All imports in App.tsx, DashboardPage.tsx, ShellLayout.tsx resolve to real files"
    - "context.tsx provides AppProvider and useApp exports matching existing consumer signatures"
    - "SettingsPage.tsx save() relies on WebSocket event, not manual reload()"
  artifacts:
    - path: "frontend/package.json"
      provides: "npm scripts and dependency declarations"
      contains: "gsd-monitor-ui"
    - path: "frontend/tsconfig.json"
      provides: "TypeScript project configuration"
      contains: "bundler"
    - path: "frontend/vite.config.ts"
      provides: "Vite build configuration with Tailwind v4 and React plugins"
      contains: "@tailwindcss/vite"
    - path: "frontend/index.html"
      provides: "Vite entry HTML"
      contains: "/src/main.tsx"
    - path: "frontend/src/main.tsx"
      provides: "React 19 root mount with BrowserRouter"
      contains: "createRoot"
    - path: "frontend/src/context.tsx"
      provides: "AppProvider context and useApp hook"
      exports: ["AppProvider", "useApp"]
    - path: "frontend/src/Drawer.tsx"
      provides: "Slide-over drawer component for phase detail"
      exports: ["Drawer"]
    - path: "frontend/src/pages/SettingsPage.tsx"
      provides: "Settings page with WebSocket-based refresh"
      exports: ["SettingsPage"]
    - path: "frontend/src/pages/DriftPage.tsx"
      provides: "v2 placeholder stub"
      exports: ["DriftPage"]
    - path: "frontend/src/pages/QuickTasksPage.tsx"
      provides: "v2 placeholder stub"
      exports: ["QuickTasksPage"]
    - path: "frontend/src/pages/VerificationPage.tsx"
      provides: "v2 placeholder stub"
      exports: ["VerificationPage"]
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/context.tsx"
      via: "import { AppProvider }"
      pattern: "import.*AppProvider.*context"
    - from: "frontend/src/pages/DashboardPage.tsx"
      to: "frontend/src/Drawer.tsx"
      via: "import { Drawer }"
      pattern: "import.*Drawer.*Drawer"
    - from: "frontend/index.html"
      to: "frontend/src/main.tsx"
      via: "script src"
      pattern: "/src/main.tsx"
---

<objective>
Reconstruct missing frontend config files and source files so the entire source tree compiles.

Purpose: The master branch is missing 4 config files (package.json, tsconfig.json, vite.config.ts, index.html) and 7 source files (main.tsx, context.tsx, Drawer.tsx, SettingsPage.tsx, DriftPage.tsx, QuickTasksPage.tsx, VerificationPage.tsx). Without these, tsc -b fails and no fresh bundle can be built. This plan creates all missing files.

Output: A complete, compilable frontend source tree in frontend/.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-frontend-source-completion/07-RESEARCH.md

<interfaces>
<!-- Existing source files the executor must understand -->

From frontend/src/App.tsx:
```typescript
import { AppProvider } from "./context";
import { ShellLayout } from "./ShellLayout";
import { DashboardPage } from "./pages/DashboardPage";
import { DriftPage } from "./pages/DriftPage";
import { QuickTasksPage } from "./pages/QuickTasksPage";
import { VerificationPage } from "./pages/VerificationPage";
import { SettingsPage } from "./pages/SettingsPage";
import { DocsPage } from "./pages/DocsPage";
```

From frontend/src/pages/DashboardPage.tsx (lines 1-6):
```typescript
import { useMemo, useState } from "react";
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";
import { useApp } from "../context";
import { Drawer } from "../Drawer";
import type { PhasePayload } from "../api";
```

From frontend/src/ShellLayout.tsx (lines 1-4):
```typescript
import { NavLink } from "react-router-dom";
import { useMemo, type ReactNode } from "react";
import { useApp } from "./context";
import type { GroupPayload, SegmentPayload, WorktreeInfo } from "./api";
```

From frontend/src/index.css:
```css
@import "tailwindcss";
body { font-family: "Segoe UI", system-ui, sans-serif; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconstruct frontend config files and create all missing source files</name>
  <files>
    frontend/package.json,
    frontend/tsconfig.json,
    frontend/vite.config.ts,
    frontend/index.html,
    frontend/src/main.tsx,
    frontend/src/context.tsx,
    frontend/src/Drawer.tsx,
    frontend/src/pages/SettingsPage.tsx,
    frontend/src/pages/DriftPage.tsx,
    frontend/src/pages/QuickTasksPage.tsx,
    frontend/src/pages/VerificationPage.tsx
  </files>
  <read_first>
    frontend/src/App.tsx,
    frontend/src/ShellLayout.tsx,
    frontend/src/pages/DashboardPage.tsx,
    frontend/src/pages/DocsPage.tsx,
    frontend/src/api.ts,
    frontend/src/index.css,
    .planning/phases/07-frontend-source-completion/07-RESEARCH.md
  </read_first>
  <action>
Create 11 files in this exact order:

**1. frontend/package.json** — Reconstruct from CLAUDE.md spec:
```json
{
  "name": "gsd-monitor-ui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.0.0",
    "react-markdown": "^10.0.0",
    "remark-gfm": "^4.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.9.0",
    "vite": "^6.0.0"
  }
}
```

**2. frontend/tsconfig.json** — Strict mode, ES2022, bundler resolution, path alias:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "noEmit": true,
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["src"]
}
```

**3. frontend/vite.config.ts** — Tailwind v4 plugin, React plugin, dev proxy:
```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    proxy: {
      "/api": "http://127.0.0.1:8765",
      "/ws": { target: "ws://127.0.0.1:8765", ws: true },
    },
  },
});
```

**4. frontend/index.html** — Standard Vite entry referencing /src/main.tsx:
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>GSD Monitor</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**5. frontend/src/main.tsx** — React 19 createRoot with BrowserRouter wrapping App:
```typescript
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";
import "./index.css";

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </StrictMode>,
);
```
Note: App.tsx uses `export default function App()`, so import as default.

**6. frontend/src/context.tsx** — Cherry-pick from worktree branch using:
```
git checkout worktree-agent-a9b06372 -- frontend/src/context.tsx
```
Do NOT use `git merge`. Only extract this single file. After checkout, verify the file exports `AppProvider` and `useApp` by reading it.

**7. frontend/src/pages/SettingsPage.tsx** — Cherry-pick from worktree branch using:
```
git checkout worktree-agent-a9b06372 -- frontend/src/pages/SettingsPage.tsx
```
After checkout, verify it does NOT call `reload()` after save (it should rely on WebSocket `projects_updated` event per PERF-04).

**8. frontend/src/Drawer.tsx** — Create the slide-over drawer component. Props: `open: boolean`, `onClose: () => void`, `title: string`, `children?: ReactNode`. When `open` is false, return null. When open, render a fixed overlay with bg-black/50 backdrop, a 480px-wide aside panel on the right with VS Code dark theme colors (bg-[#1e1e1e], border-[#474747], text-[#cccccc]), a header bar with title and close button, and a scrollable content area for children. Use the exact implementation from the RESEARCH.md `Stub Component Pattern` section.

**9. frontend/src/pages/DriftPage.tsx** — Minimal v2 placeholder stub:
```typescript
export function DriftPage() {
  return <div className="p-8 text-[#858585]">Drift detection coming in v2.</div>;
}
```

**10. frontend/src/pages/QuickTasksPage.tsx** — Minimal v2 placeholder stub:
```typescript
export function QuickTasksPage() {
  return <div className="p-8 text-[#858585]">Quick tasks coming in v2.</div>;
}
```

**11. frontend/src/pages/VerificationPage.tsx** — Minimal v2 placeholder stub:
```typescript
export function VerificationPage() {
  return <div className="p-8 text-[#858585]">Verification detail coming in v2.</div>;
}
```

**CRITICAL anti-patterns:**
- Do NOT run `npm install` — all packages are already in node_modules
- Do NOT run `git merge worktree-agent-a9b06372` — it would overwrite api.ts (removing stateCurrentPosition) and DashboardPage.tsx
- Do NOT modify App.tsx, DashboardPage.tsx, DocsPage.tsx, ShellLayout.tsx, or api.ts — they are already correct
- After checking out context.tsx, run `git diff HEAD -- frontend/src/api.ts` to confirm api.ts was NOT changed by the checkout

**Typography check:** Run `ls frontend/node_modules/@tailwindcss/` and check if `typography/` directory exists. If it does NOT exist, run `npm install @tailwindcss/typography` (this is the one exception to the no-install rule). DashboardPage.tsx and DocsPage.tsx use `prose prose-invert` classes that require this plugin.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains `"name": "gsd-monitor-ui"`
    - frontend/package.json contains `"build": "tsc -b && vite build"`
    - frontend/tsconfig.json contains `"moduleResolution": "bundler"`
    - frontend/tsconfig.json contains `"strict": true`
    - frontend/vite.config.ts contains `import tailwindcss from "@tailwindcss/vite"`
    - frontend/vite.config.ts contains `"/api": "http://127.0.0.1:8765"`
    - frontend/index.html contains `/src/main.tsx`
    - frontend/src/main.tsx contains `createRoot(document.getElementById("root")!)`
    - frontend/src/main.tsx contains `import App from "./App"`
    - frontend/src/context.tsx contains `export function AppProvider`
    - frontend/src/context.tsx contains `export function useApp` or `export const useApp`
    - frontend/src/Drawer.tsx contains `export function Drawer(`
    - frontend/src/Drawer.tsx contains `open: boolean`
    - frontend/src/pages/SettingsPage.tsx contains `export function SettingsPage`
    - frontend/src/pages/SettingsPage.tsx does NOT contain `reload()` call after save
    - frontend/src/pages/DriftPage.tsx contains `export function DriftPage`
    - frontend/src/pages/QuickTasksPage.tsx contains `export function QuickTasksPage`
    - frontend/src/pages/VerificationPage.tsx contains `export function VerificationPage`
    - `cd frontend && npx tsc -b` exits with code 0
    - `git diff HEAD -- frontend/src/api.ts` shows no changes (api.ts untouched)
  </acceptance_criteria>
  <done>All 11 files exist, tsc -b passes with zero errors, no existing source files were modified</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc -b` exits 0 (zero type errors)
- All imports in App.tsx resolve: context.tsx, Drawer.tsx, all 6 page components
- `git diff HEAD -- frontend/src/api.ts frontend/src/pages/DashboardPage.tsx frontend/src/pages/DocsPage.tsx frontend/src/ShellLayout.tsx frontend/src/App.tsx` shows no changes
</verification>

<success_criteria>
- 11 files created (4 config + 7 source)
- tsc -b passes with zero errors
- No existing source files modified
- context.tsx exports AppProvider and useApp
- SettingsPage.tsx does not call reload() after save
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-source-completion/07-01-SUMMARY.md`
</output>
