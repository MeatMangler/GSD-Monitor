---
phase: 02-visual-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/ShellLayout.tsx
autonomous: true
requirements:
  - DASH-04

must_haves:
  truths:
    - "Sidebar renders with VS Code Dark+ background (#252526), not zinc-900"
    - "All sidebar borders use #474747 instead of zinc-800"
    - "Nav active state uses #2a2d2e background, not zinc-800"
    - "Primary text is #cccccc, muted text is #858585 throughout sidebar"
    - "Worktree badge and tooltip use VS Code hex tokens"
    - "Outer shell wrapper uses #1e1e1e background"
  artifacts:
    - path: "frontend/src/ShellLayout.tsx"
      provides: "Sidebar with VS Code Dark+ color tokens"
      min_lines: 100
  key_links:
    - from: "frontend/src/ShellLayout.tsx"
      to: "VS Code token table"
      via: "Arbitrary Tailwind hex values"
      pattern: "bg-\\[#252526\\]|bg-\\[#1e1e1e\\]|border-\\[#474747\\]"
---

<objective>
Replace all zinc-scale Tailwind classes in ShellLayout.tsx with VS Code Dark+ hex tokens using Tailwind v4 arbitrary value syntax.

Purpose: Complete the VS Code dark theme migration for the sidebar/shell — making it visually consistent with the DashboardPage token changes in Plan 01.

Output: Updated `ShellLayout.tsx` with all color classes swapped to VS Code hex tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-redesign/02-CONTEXT.md
@.planning/phases/02-visual-redesign/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap all zinc-scale color classes to VS Code Dark+ hex tokens in ShellLayout.tsx</name>
  <files>frontend/src/ShellLayout.tsx</files>

  <read_first>
    - frontend/src/ShellLayout.tsx
    - .planning/phases/02-visual-redesign/02-UI-SPEC.md
    - .planning/phases/02-visual-redesign/02-CONTEXT.md
  </read_first>

  <action>
This is a mechanical class-string replacement task. No JSX structural changes. No logic changes. Only `className` attribute values change.

Per D-13 (locked), apply the following token swap table from the UI-SPEC. Every replacement is listed exhaustively:

**Outer shell wrapper** (line 49, `<div className="flex h-screen overflow-hidden bg-zinc-950">`):
- `bg-zinc-950` -> `bg-[#1e1e1e]`

**Aside element** (line 50, `<aside ...>`):
- `border-zinc-800` -> `border-[#474747]`
- `bg-zinc-900/80` -> `bg-[#252526]`

**Header div** (line 51, `<div className="border-b border-zinc-800 p-3">`):
- `border-zinc-800` -> `border-[#474747]`

**App title h1** (line 52):
- `text-zinc-100` -> `text-[#cccccc]`

**Subtitle p** (line 53):
- `text-zinc-500` -> `text-[#858585]`

**Loading text** (line 56):
- `text-zinc-500` -> `text-[#858585]`

**Labels** (lines 58, 100, 130 — all `<label>` elements):
- `text-zinc-400` -> `text-[#858585]`

**All `<select>` elements** (lines 59-60, 101-102, 132-133):
- `border-zinc-700` -> `border-[#474747]`
- `bg-zinc-950` -> `bg-[#252526]`

**Worktree badge span** (line 82):
- `bg-zinc-700` -> `bg-[#2a2d2e]`
- `text-zinc-300` -> `text-[#858585]`

**Worktree tooltip container div** (line 85):
- `border-zinc-700` -> `border-[#474747]`
- `bg-zinc-900` -> `bg-[#252526]`

**Worktree branch text** (line 88):
- `text-zinc-300` -> `text-[#cccccc]`

**Worktree path segment** (line 89):
- `text-zinc-500` -> `text-[#858585]`

**Worktree "main" indicator** (line 91):
- `text-zinc-600` -> `text-[#858585]`

**NavLink active state** (line 168):
- `bg-zinc-800 text-white` -> `bg-[#2a2d2e] text-[#cccccc]`

**NavLink inactive state** (line 168):
- `text-zinc-400 hover:bg-zinc-800/60 hover:text-zinc-200` -> `text-[#858585] hover:bg-[#2a2d2e] hover:text-[#cccccc]`

**IMPORTANT:**
- Do NOT change any JSX structure, event handlers, or logic — only className strings.
- Do NOT touch the `text-red-400` error text class — leave it unchanged per UI-SPEC.
- Do NOT create a `tailwind.config.js` file.
- The `text-sm` on select elements already exists — leave it. Only swap color/bg/border classes.
  </action>

  <verify>
    <automated>cd D:/gsd-monitor-py/frontend && npx tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - ShellLayout.tsx contains `bg-[#1e1e1e]` on the outer wrapper div
    - ShellLayout.tsx contains `bg-[#252526]` on the aside element
    - ShellLayout.tsx contains `border-[#474747]` (at least 4 occurrences — aside, header, selects, tooltip)
    - ShellLayout.tsx contains `text-[#cccccc]` for app title and branch text
    - ShellLayout.tsx contains `text-[#858585]` for labels, subtitle, muted text
    - ShellLayout.tsx contains `bg-[#2a2d2e]` for NavLink active state and worktree badge
    - ShellLayout.tsx contains `hover:bg-[#2a2d2e]` for NavLink hover
    - ShellLayout.tsx contains `hover:text-[#cccccc]` for NavLink hover text
    - ShellLayout.tsx does NOT contain `bg-zinc-950` or `bg-zinc-900` or `border-zinc-800` or `border-zinc-700` or `text-zinc-100` or `text-zinc-400` or `text-zinc-500` or `text-zinc-300` or `text-zinc-600` or `text-white` or `bg-zinc-800` or `bg-zinc-700` or `hover:bg-zinc-800` or `hover:text-zinc-200`
    - ShellLayout.tsx STILL contains `text-red-400` (error display, unchanged)
    - `npx tsc -b --noEmit` exits with code 0
  </acceptance_criteria>

  <done>
    Every zinc-scale color class in ShellLayout.tsx has been replaced with the corresponding VS Code Dark+ hex token. The sidebar, nav, selects, worktree badge, and tooltip all use the new palette. Error text remains red. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc -b --noEmit` — zero errors
2. `cd frontend && npx vite build` — builds successfully
3. Visual: Sidebar background is #252526, outer background is #1e1e1e
4. Visual: Active nav item has #2a2d2e background, inactive items are #858585
5. Visual: Worktree badge uses #2a2d2e bg with #858585 text
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- Vite build succeeds
- No zinc-* color classes remain in ShellLayout.tsx (except no zinc classes should remain at all)
- text-red-400 is preserved for error display
- All sidebar elements use VS Code Dark+ hex tokens per the token table in UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-redesign/02-02-SUMMARY.md`
</output>
