---
phase: 08-phase01-verification-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-worktree-deduplication/01-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [WRKTR-01, WRKTR-02, WRKTR-05]
must_haves:
  truths:
    - "Phase 01 VERIFICATION.md exists and documents WRKTR-01 with code evidence showing single ProjectGroup per canonical root"
    - "Phase 01 VERIFICATION.md documents WRKTR-02 with code evidence showing .git file detection and gitdir: pointer resolution"
    - "Phase 01 VERIFICATION.md documents WRKTR-05 with code evidence showing is_primary field on WorktreeInfo"
    - "REQUIREMENTS.md checkboxes for WRKTR-01, WRKTR-02, WRKTR-05 are [x] and traceability shows Complete"
  artifacts:
    - path: ".planning/phases/01-worktree-deduplication/01-VERIFICATION.md"
      provides: "Verification report documenting WRKTR-01, WRKTR-02, WRKTR-05 with code evidence"
      contains: "WRKTR-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes and traceability"
      contains: "[x] **WRKTR-01**"
  key_links:
    - from: ".planning/phases/01-worktree-deduplication/01-VERIFICATION.md"
      to: "gsd_monitor/services/project_discovery.py"
      via: "Code evidence references with line numbers"
      pattern: "project_discovery\\.py"
---

<objective>
Write the Phase 01 VERIFICATION.md to close the three orphaned requirements (WRKTR-01, WRKTR-02, WRKTR-05) with concrete code evidence, then update REQUIREMENTS.md to mark them satisfied.

Purpose: These requirements were implemented in Phase 01 but never had a verification artifact written, leaving them as "orphaned" in the audit. This plan closes that gap.
Output: 01-VERIFICATION.md with code evidence for all three requirements, updated REQUIREMENTS.md checkboxes and traceability.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/01-worktree-deduplication/01-01-PLAN.md
@.planning/phases/01-worktree-deduplication/01-02-PLAN.md

@gsd_monitor/services/project_discovery.py
@gsd_monitor/services/git_service.py
@gsd_monitor/api/app.py
@frontend/src/api.ts

<interfaces>
<!-- Existing verification format — use 04-VERIFICATION.md as template -->
<!-- Key code locations for evidence gathering: -->

From gsd_monitor/services/project_discovery.py:
```python
def _resolve_canonical_root(repo_dir: Path) -> Path:
    """Return canonical repo root. For linked worktrees, resolves .git file pointer."""
    # Lines ~48-69: reads .git file, parses gitdir: pointer, resolves canonical root

@dataclass
class WorktreeInfo:
    path: str
    branch: str
    is_primary: bool
    # Lines ~87-90

@dataclass
class ProjectGroup:
    # ...
    worktrees: list[WorktreeInfo] = field(default_factory=list)
    # Line ~102

def discover_groups(self, scan_roots: list[str]) -> list[ProjectGroup]:
    # Lines ~109+: uses _resolve_canonical_root for deduplication
    # by_repo_worktrees dict accumulates worktree info
    # canon_key used for keying instead of raw repo_dir
```

From gsd_monitor/api/app.py:
```python
def _group_to_json(g: ProjectGroup) -> dict[str, Any]:
    # Serializes worktrees with isPrimary field
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 01 VERIFICATION.md with code evidence</name>
  <files>.planning/phases/01-worktree-deduplication/01-VERIFICATION.md</files>
  <read_first>
    .planning/phases/04-performance-correctness/04-VERIFICATION.md,
    .planning/phases/01-worktree-deduplication/01-01-PLAN.md,
    .planning/phases/01-worktree-deduplication/01-02-PLAN.md,
    gsd_monitor/services/project_discovery.py,
    gsd_monitor/services/git_service.py,
    gsd_monitor/api/app.py,
    frontend/src/api.ts,
    frontend/src/pages/DashboardPage.tsx,
    tests/test_worktree_resolution.py
  </read_first>
  <action>
    Create `.planning/phases/01-worktree-deduplication/01-VERIFICATION.md` following the same structure as `04-VERIFICATION.md`. The file must contain:

    **Frontmatter:**
    ```yaml
    ---
    phase: 01-worktree-deduplication
    verified: {current ISO timestamp}
    status: passed
    score: 5/5 must-haves verified
    ---
    ```

    **Goal Achievement section** with Observable Truths table. Verify these 5 truths by reading actual source code and reporting line numbers:

    1. **WRKTR-01 — Single entry per canonical root**: `project_discovery.py` `discover_groups()` uses `_resolve_canonical_root(repo_dir)` to compute `canon_key = str(canonical)` and keys `by_repo` dict by `canon_key`. GSD-1 scan has guard `if canon_key in by_repo: continue` preventing duplicate segments. Evidence: grep for `canon_key` in discover_groups, report exact line numbers.

    2. **WRKTR-02 — .git file detection and gitdir: pointer resolution**: `_resolve_canonical_root()` checks `dot_git.is_file()`, reads content, checks `content.startswith("gitdir:")`, parses the path, resolves to canonical via `gitdir_path.parent.parent.parent`. Evidence: report the function at its exact line numbers, show the `.git` file check and `gitdir:` parsing.

    3. **WRKTR-03 — Worktree count badge** (already [x] but include for completeness): Frontend `DashboardPage.tsx` or sidebar shows worktree count badge when `worktrees.length > 1`. Evidence: grep frontend for `worktrees` rendering.

    4. **WRKTR-04 — Hover/click shows worktree list** (already [x]): Frontend tooltip or dropdown shows worktree branch/directory names. Evidence: grep frontend for worktree tooltip/hover rendering.

    5. **WRKTR-05 — Active worktree indicator (is_primary)**: `WorktreeInfo` dataclass has `is_primary: bool` field (project_discovery.py line ~90). `is_primary` is set to `True` when `(repo_dir / ".git").is_dir()` (i.e., the main worktree has a `.git` directory, linked worktrees have a `.git` file). API serializes as `isPrimary` in `_group_to_json`. Frontend `api.ts` has `isPrimary` on the `WorktreePayload` interface. Evidence: report exact lines for dataclass field, is_primary assignment, API serialization, and frontend type.

    **Required Artifacts section** with table showing:
    - `gsd_monitor/services/project_discovery.py` — `_resolve_canonical_root`, `WorktreeInfo`, deduplication in `discover_groups`
    - `gsd_monitor/services/git_service.py` — `get_branch_name` method
    - `gsd_monitor/api/app.py` — `worktrees` serialization in `_group_to_json`
    - `frontend/src/api.ts` — `WorktreePayload` interface with `isPrimary`
    - `frontend/src/pages/DashboardPage.tsx` — worktree badge rendering
    - `tests/test_worktree_resolution.py` — unit tests for canonical root resolution

    **Key Link Verification section** showing wiring from discovery -> API -> frontend.

    **Behavioral Spot-Checks section** with commands:
    - `python -c "from gsd_monitor.services.project_discovery import _resolve_canonical_root, WorktreeInfo; print('OK')"` — imports succeed
    - `python -m pytest tests/test_worktree_resolution.py -x` — tests pass
    - `python -c "from gsd_monitor.services.git_service import GitService; print(GitService().get_branch_name(''))"` — prints "unknown"

    **Requirements Coverage section** mapping WRKTR-01 through WRKTR-05 to plan 01-01 or 01-02 with SATISFIED status and evidence summary.

    IMPORTANT: All line numbers and evidence MUST come from actually reading the source files (via read_first), not from assumptions. Use `grep -n` to find exact line numbers during execution.
  </action>
  <verify>
    <automated>test -f "D:/gsd-monitor-py/.planning/phases/01-worktree-deduplication/01-VERIFICATION.md" && grep -q "WRKTR-01" "D:/gsd-monitor-py/.planning/phases/01-worktree-deduplication/01-VERIFICATION.md" && grep -q "WRKTR-02" "D:/gsd-monitor-py/.planning/phases/01-worktree-deduplication/01-VERIFICATION.md" && grep -q "WRKTR-05" "D:/gsd-monitor-py/.planning/phases/01-worktree-deduplication/01-VERIFICATION.md" && grep -q "SATISFIED" "D:/gsd-monitor-py/.planning/phases/01-worktree-deduplication/01-VERIFICATION.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-worktree-deduplication/01-VERIFICATION.md` exists
    - File contains `WRKTR-01` with evidence referencing `canon_key` and `_resolve_canonical_root`
    - File contains `WRKTR-02` with evidence referencing `dot_git.is_file()` and `gitdir:`
    - File contains `WRKTR-05` with evidence referencing `is_primary: bool` and `isPrimary`
    - File contains `status: passed` in frontmatter
    - File contains at least 5 rows in Requirements Coverage table with `SATISFIED` status
    - File contains Behavioral Spot-Checks section with at least 3 commands
  </acceptance_criteria>
  <done>
    Phase 01 VERIFICATION.md exists with concrete code evidence (line numbers, function names, grep patterns) for all 5 WRKTR requirements, confirming WRKTR-01, WRKTR-02, and WRKTR-05 are satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md to mark WRKTR-01, WRKTR-02, WRKTR-05 as satisfied</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` with these exact changes:

    **1. Checkbox updates (lines 10-14):**
    Change line 10 from:
    `- [ ] **WRKTR-01**: Project dropdown shows exactly one entry per canonical repo root...`
    to:
    `- [x] **WRKTR-01**: Project dropdown shows exactly one entry per canonical repo root...`

    Change line 11 from:
    `- [ ] **WRKTR-02**: App detects git worktrees by checking if `.git` is a file...`
    to:
    `- [x] **WRKTR-02**: App detects git worktrees by checking if `.git` is a file...`

    Change line 14 from:
    `- [ ] **WRKTR-05**: An indicator marks which worktree is currently active...`
    to:
    `- [x] **WRKTR-05**: An indicator marks which worktree is currently active...`

    **2. Traceability table updates (lines 60-64):**
    Change WRKTR-01 row from:
    `| WRKTR-01 | Phase 8 | Pending (orphaned — verification missing) |`
    to:
    `| WRKTR-01 | Phase 1 | Complete (01-01, verified in 01-VERIFICATION.md) |`

    Change WRKTR-02 row from:
    `| WRKTR-02 | Phase 8 | Pending (orphaned — verification missing) |`
    to:
    `| WRKTR-02 | Phase 1 | Complete (01-01, verified in 01-VERIFICATION.md) |`

    Change WRKTR-05 row from:
    `| WRKTR-05 | Phase 8 | Pending (orphaned — verification missing) |`
    to:
    `| WRKTR-05 | Phase 1 | Complete (01-01, verified in 01-VERIFICATION.md) |`

    **3. Coverage summary update (lines 80-81):**
    Change from:
    `- Satisfied: 13 (\`[x]\`), 3 pending (WRKTR-01, WRKTR-02, WRKTR-05 — closing in Phase 8)`
    to:
    `- Satisfied: 16 (\`[x]\`), 0 pending`

    **4. Last updated line (line 85):**
    Change to:
    `*Last updated: 2026-04-04 — WRKTR-01, WRKTR-02, WRKTR-05 verified and closed (Phase 8 gap closure)*`
  </action>
  <verify>
    <automated>grep -c "\[x\]" "D:/gsd-monitor-py/.planning/REQUIREMENTS.md" | grep -q "16" && grep -q "Satisfied: 16" "D:/gsd-monitor-py/.planning/REQUIREMENTS.md" && grep -q "Complete (01-01" "D:/gsd-monitor-py/.planning/REQUIREMENTS.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `[x] **WRKTR-01**` (not `[ ]`)
    - `.planning/REQUIREMENTS.md` contains `[x] **WRKTR-02**` (not `[ ]`)
    - `.planning/REQUIREMENTS.md` contains `[x] **WRKTR-05**` (not `[ ]`)
    - Traceability table shows `Complete (01-01, verified in 01-VERIFICATION.md)` for WRKTR-01, WRKTR-02, WRKTR-05
    - Coverage summary shows `Satisfied: 16` and `0 pending`
    - `grep -c "\[x\]" .planning/REQUIREMENTS.md` returns 16
  </acceptance_criteria>
  <done>
    All 16 v1 requirements are marked [x] in REQUIREMENTS.md. Traceability table shows Complete for WRKTR-01, WRKTR-02, WRKTR-05. Zero pending requirements remain.
  </done>
</task>

</tasks>

<verification>
1. `test -f .planning/phases/01-worktree-deduplication/01-VERIFICATION.md` — file exists
2. `grep -c "SATISFIED" .planning/phases/01-worktree-deduplication/01-VERIFICATION.md` — returns 5 (one per WRKTR requirement)
3. `grep -c "\[x\]" .planning/REQUIREMENTS.md` — returns 16
4. `grep "WRKTR-01.*Complete" .planning/REQUIREMENTS.md` — matches
</verification>

<success_criteria>
- 01-VERIFICATION.md exists with code evidence for WRKTR-01, WRKTR-02, WRKTR-05
- All 16 v1 requirements are [x] in REQUIREMENTS.md
- Traceability table has zero Pending rows
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase01-verification-cleanup/08-01-SUMMARY.md`
</output>
