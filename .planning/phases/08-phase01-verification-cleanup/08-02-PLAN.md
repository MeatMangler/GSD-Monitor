---
phase: 08-phase01-verification-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - gsd_monitor/services/project_discovery.py
  - gsd_monitor/parsers/quick_task.py
autonomous: true
requirements: [WRKTR-01, WRKTR-02, WRKTR-05]
must_haves:
  truths:
    - "ROADMAP.md Phase 8 row shows correct plan count and status"
    - "STATE.md progress reflects all completed phases accurately"
    - "All datetime.utcfromtimestamp() calls in project_discovery.py are replaced with timezone-aware equivalents"
    - "All datetime.utcfromtimestamp() calls in quick_task.py are replaced with timezone-aware equivalents"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Accurate phase metadata"
      contains: "Phase 8"
    - path: ".planning/STATE.md"
      provides: "Current project state"
      contains: "Phase: 08"
    - path: "gsd_monitor/services/project_discovery.py"
      provides: "Timezone-aware datetime usage"
      contains: "datetime.fromtimestamp"
    - path: "gsd_monitor/parsers/quick_task.py"
      provides: "Timezone-aware datetime usage"
      contains: "datetime.fromtimestamp"
  key_links:
    - from: "gsd_monitor/services/project_discovery.py"
      to: "datetime.timezone.utc"
      via: "fromtimestamp with tz=timezone.utc"
      pattern: "fromtimestamp.*timezone\\.utc"
---

<objective>
Fix stale planning metadata (STATE.md and ROADMAP.md) and replace deprecated datetime.utcfromtimestamp() with timezone-aware equivalents across the codebase.

Purpose: STATE.md and ROADMAP.md have stale Phase 6/7 metadata. Python 3.12 deprecates datetime.utcfromtimestamp() — replacing with datetime.fromtimestamp(ts, tz=timezone.utc) eliminates the deprecation warning and produces timezone-aware datetimes.
Output: Updated STATE.md, ROADMAP.md with current metadata; project_discovery.py and quick_task.py using timezone-aware datetime calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update STATE.md and ROADMAP.md with accurate Phase 8 metadata</name>
  <files>
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/STATE.md,
    .planning/ROADMAP.md
  </read_first>
  <action>
    **STATE.md updates:**

    1. Change frontmatter `stopped_at` from `Completed 07-02-PLAN.md` to `Executing Phase 08`
    2. Change frontmatter `progress.completed_phases` from `6` to `7` (Phase 7 is complete)
    3. Change frontmatter `progress.total_plans` from `13` to `15` (13 existing + 2 new Phase 8 plans)
    4. Change frontmatter `progress.completed_plans` from `11` to `13` (Phases 1-7 all complete = 13 plans)
    5. Change frontmatter `progress.percent` from `15` to `87` (13/15 = 87%)
    6. Change `**Current focus:**` from `Phase 07 — frontend-source-completion` to `Phase 08 — phase01-verification-cleanup`
    7. Change `Plan: Not started` to `Plan: Executing`
    8. Change `Status: Phase complete — ready for verification` to `Status: Executing Phase 08`
    9. Update `Progress:` bar to `[████████░░] 87%`

    **ROADMAP.md updates:**

    1. Under `### Phase 8:` section, change `**Plans**: 0 plans` to `**Plans**: 2 plans`
    2. Add plan list under Plans line:
    ```
    Plans:
    - [ ] 08-01-PLAN.md — Write Phase 01 VERIFICATION.md and update REQUIREMENTS.md
    - [ ] 08-02-PLAN.md — Fix stale metadata and Python datetime deprecation
    ```
    3. In the Progress table at the bottom, update Phase 8 row from:
    `| 8. Phase 01 Verification & Metadata Cleanup | 0/0 | Pending | |`
    to:
    `| 8. Phase 01 Verification & Metadata Cleanup | 0/2 | Active | |`
  </action>
  <verify>
    <automated>grep -q "completed_phases: 7" "D:/gsd-monitor-py/.planning/STATE.md" && grep -q "2 plans" "D:/gsd-monitor-py/.planning/ROADMAP.md" && grep -q "08-01-PLAN" "D:/gsd-monitor-py/.planning/ROADMAP.md" && grep -q "08-02-PLAN" "D:/gsd-monitor-py/.planning/ROADMAP.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter contains `stopped_at: Executing Phase 08`
    - STATE.md frontmatter contains `completed_phases: 7`
    - STATE.md frontmatter contains `completed_plans: 13`
    - STATE.md frontmatter contains `percent: 87`
    - STATE.md body contains `Current focus: Phase 08`
    - ROADMAP.md Phase 8 section contains `**Plans**: 2 plans`
    - ROADMAP.md contains `08-01-PLAN.md` and `08-02-PLAN.md` in plan list
    - ROADMAP.md Progress table Phase 8 row shows `0/2` and `Active`
  </acceptance_criteria>
  <done>
    STATE.md and ROADMAP.md accurately reflect Phase 8 planning with correct plan counts, completion percentages, and active status.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace deprecated datetime.utcfromtimestamp() with timezone-aware equivalent</name>
  <files>
    gsd_monitor/services/project_discovery.py,
    gsd_monitor/parsers/quick_task.py
  </files>
  <read_first>
    gsd_monitor/services/project_discovery.py,
    gsd_monitor/parsers/quick_task.py
  </read_first>
  <action>
    Python 3.12 deprecated `datetime.utcfromtimestamp()`. Replace all occurrences with `datetime.fromtimestamp(ts, tz=timezone.utc)` which produces timezone-aware UTC datetimes.

    **In `gsd_monitor/services/project_discovery.py`:**

    1. Change the import on line 10 from:
       `from datetime import datetime`
       to:
       `from datetime import datetime, timezone`

    2. Replace all 7 occurrences of `datetime.utcfromtimestamp(...)` with `datetime.fromtimestamp(..., tz=timezone.utc)`:

       - Line 314: `lu = datetime.utcfromtimestamp(sp.stat().st_mtime)` -> `lu = datetime.fromtimestamp(sp.stat().st_mtime, tz=timezone.utc)`
       - Line 376: `datetime.utcfromtimestamp(f.stat().st_mtime) for f in summary_files` -> `datetime.fromtimestamp(f.stat().st_mtime, tz=timezone.utc) for f in summary_files`
       - Line 379: `last_updated = datetime.utcfromtimestamp(latest_plan.stat().st_mtime)` -> `last_updated = datetime.fromtimestamp(latest_plan.stat().st_mtime, tz=timezone.utc)`
       - Line 384: `datetime.utcfromtimestamp(f.stat().st_mtime) for f in plan_files` -> `datetime.fromtimestamp(f.stat().st_mtime, tz=timezone.utc) for f in plan_files`
       - Line 529: `lu = datetime.utcfromtimestamp(state_path.stat().st_mtime)` -> `lu = datetime.fromtimestamp(state_path.stat().st_mtime, tz=timezone.utc)`
       - Line 593: `last_updated = datetime.utcfromtimestamp(summary_file.stat().st_mtime)` -> `last_updated = datetime.fromtimestamp(summary_file.stat().st_mtime, tz=timezone.utc)`
       - Line 609: `plan_write_time = datetime.utcfromtimestamp(plan_file.stat().st_mtime)` -> `plan_write_time = datetime.fromtimestamp(plan_file.stat().st_mtime, tz=timezone.utc)`

    **In `gsd_monitor/parsers/quick_task.py`:**

    1. Change the import on line 6 from:
       `from datetime import datetime`
       to:
       `from datetime import datetime, timezone`

    2. Replace both occurrences on lines 46-47:
       - `st_ct = datetime.utcfromtimestamp(fp.stat().st_ctime)` -> `st_ct = datetime.fromtimestamp(fp.stat().st_ctime, tz=timezone.utc)`
       - `st_mt = datetime.utcfromtimestamp(fp.stat().st_mtime)` -> `st_mt = datetime.fromtimestamp(fp.stat().st_mtime, tz=timezone.utc)`

    **Verification note:** `git_service.py` already imports `timezone` and uses `datetime.fromtimestamp(..., tz=timezone.utc)` — no changes needed there.
  </action>
  <verify>
    <automated>cd "D:/gsd-monitor-py" && python -c "import ast; ast.parse(open('gsd_monitor/services/project_discovery.py').read()); ast.parse(open('gsd_monitor/parsers/quick_task.py').read()); print('syntax OK')" && ! grep -q "utcfromtimestamp" gsd_monitor/services/project_discovery.py && ! grep -q "utcfromtimestamp" gsd_monitor/parsers/quick_task.py && grep -q "timezone" gsd_monitor/services/project_discovery.py && grep -q "timezone" gsd_monitor/parsers/quick_task.py && python -m pytest tests/ -x --tb=short && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `gsd_monitor/services/project_discovery.py` contains `from datetime import datetime, timezone`
    - `gsd_monitor/services/project_discovery.py` does NOT contain `utcfromtimestamp` (zero occurrences)
    - `gsd_monitor/services/project_discovery.py` contains `fromtimestamp` with `tz=timezone.utc` (7 occurrences)
    - `gsd_monitor/parsers/quick_task.py` contains `from datetime import datetime, timezone`
    - `gsd_monitor/parsers/quick_task.py` does NOT contain `utcfromtimestamp` (zero occurrences)
    - `gsd_monitor/parsers/quick_task.py` contains `fromtimestamp` with `tz=timezone.utc` (2 occurrences)
    - `python -c "from gsd_monitor.services.project_discovery import ProjectDiscoveryService; print('OK')"` exits 0
    - `python -m pytest tests/ -x` exits 0
  </acceptance_criteria>
  <done>
    All 9 occurrences of deprecated datetime.utcfromtimestamp() replaced with datetime.fromtimestamp(ts, tz=timezone.utc). Zero deprecation warnings remain. All existing tests pass.
  </done>
</task>

</tasks>

<verification>
1. `grep -rc "utcfromtimestamp" gsd_monitor/` — returns 0 (no deprecated calls remain)
2. `grep -c "tz=timezone.utc" gsd_monitor/services/project_discovery.py` — returns 7
3. `grep -c "tz=timezone.utc" gsd_monitor/parsers/quick_task.py` — returns 2
4. `python -m pytest tests/ -x` — all tests pass
5. STATE.md contains `completed_phases: 7`
6. ROADMAP.md Phase 8 contains `2 plans`
</verification>

<success_criteria>
- Zero occurrences of datetime.utcfromtimestamp() in the codebase
- STATE.md and ROADMAP.md reflect accurate Phase 8 metadata
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase01-verification-cleanup/08-02-SUMMARY.md`
</output>
