---
phase: 12-enhanced-visibility
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - frontend/src/pages/InsightsPage.tsx
  - frontend/src/api.ts
  - frontend/src/App.tsx
  - frontend/src/ShellLayout.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04

must_haves:
  truths:
    - "User can navigate to Insights page from the sidebar nav"
    - "Requirements tab shows a table of requirements grouped by category with phase mapping and status"
    - "Unmapped requirements (gaps) are visually highlighted with a red/amber row tint"
    - "Waves tab shows only multi-wave phases with plans grouped by wave number"
    - "Archives tab shows shipped milestones in collapsed accordions that expand to show phase lists"
  artifacts:
    - path: "frontend/src/pages/InsightsPage.tsx"
      provides: "Three-tab Insights page (Requirements, Waves, Archives)"
      min_lines: 100
    - path: "frontend/src/api.ts"
      provides: "InsightsPayload interface and fetchInsights function"
      contains: "fetchInsights"
    - path: "frontend/src/App.tsx"
      provides: "Route for /insights"
      contains: "/insights"
    - path: "frontend/src/ShellLayout.tsx"
      provides: "Insights nav item"
      contains: "Insights"
  key_links:
    - from: "frontend/src/pages/InsightsPage.tsx"
      to: "frontend/src/api.ts"
      via: "calls fetchInsights() for requirements and wave data"
      pattern: "fetchInsights"
    - from: "frontend/src/pages/InsightsPage.tsx"
      to: "frontend/src/context.tsx"
      via: "reads activeProject.milestones for archive tab"
      pattern: "useApp"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/InsightsPage.tsx"
      via: "Route element renders InsightsPage"
      pattern: "InsightsPage"
---

<objective>
Frontend: Build InsightsPage with Requirements, Waves, and Archives tabs, wire routing and navigation.

Purpose: Deliver the user-facing UI for all four VIS requirements. Requirements tab shows traceability with gap highlighting (VIS-01, VIS-04). Waves tab shows plan parallelization (VIS-02). Archives tab shows shipped milestones (VIS-03). Per D-01, all three live in a single tabbed page.

Output: InsightsPage.tsx component, updated api.ts with fetch function and interfaces, updated routing and nav.
</objective>

<execution_context>
@C:/Users/mike/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/mike/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-enhanced-visibility/12-01-SUMMARY.md
</context>

## Artifacts this phase produces

### New files
- `frontend/src/pages/InsightsPage.tsx` — Three-tab page component

### New symbols (TypeScript)
- `InsightsPage` (named export, function component)
- `RequirementEntryPayload` interface in api.ts
- `PlanWaveEntry` interface in api.ts
- `PhaseWavePayload` interface in api.ts
- `InsightsPayload` interface in api.ts
- `fetchInsights(planningPath: string)` async function in api.ts

### Modified symbols
- `MilestonePayload` in api.ts — adds `is_archived?: boolean` and `completion_date?: string | null`
- `nav` array in ShellLayout.tsx — adds Insights entry
- `App` component in App.tsx — adds /insights route

<tasks>

<task type="auto">
  <name>Task 1: API types and fetch function</name>
  <files>frontend/src/api.ts</files>
  <read_first>
    - frontend/src/api.ts (current interfaces and fetch functions — see fetchQuickTasks for the pattern to follow, and MilestonePayload for fields to extend)
  </read_first>
  <action>
    In frontend/src/api.ts, add four new interfaces and one fetch function. Also extend MilestonePayload.

    Add interfaces (after the existing QuickTaskPayload interface):

    RequirementEntryPayload with fields: id (string), category (string), description (string), is_checked (boolean), phase (string | null), status (string | null), is_gap (boolean).

    PlanWaveEntry with fields: plan_name (string), wave (number).

    PhaseWavePayload with fields: phase_number (number), phase_title (string), plans (PlanWaveEntry[]).

    InsightsPayload with fields: requirements (RequirementEntryPayload[]), wave_phases (PhaseWavePayload[]).

    Add fetchInsights function following the fetchQuickTasks pattern exactly:
    - Takes planningPath: string parameter
    - Returns Promise of InsightsPayload
    - URL-encodes planningPath, fetches from /api/insights/{encoded}, uses noStore
    - Throws on non-ok response
    - Returns r.json() cast as Promise of InsightsPayload

    Extend MilestonePayload with two optional fields:
    - is_archived?: boolean
    - completion_date?: string | null
  </action>
  <verify>
    <automated>cd D:/gsd-monitor-py/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - api.ts contains export interface RequirementEntryPayload with 7 fields
    - api.ts contains export interface InsightsPayload with requirements and wave_phases fields
    - api.ts contains export async function fetchInsights
    - MilestonePayload has is_archived and completion_date fields
    - tsc --noEmit passes with no errors related to new types
  </acceptance_criteria>
  <done>TypeScript interfaces and fetch function for insights endpoint are defined. MilestonePayload extended with archive fields.</done>
</task>

<task type="auto">
  <name>Task 2: InsightsPage component with three tabs</name>
  <files>frontend/src/pages/InsightsPage.tsx</files>
  <read_first>
    - frontend/src/pages/DriftPage.tsx (page structure pattern: useApp, loading state, empty state, card layout)
    - frontend/src/pages/VerificationPage.tsx (collapsible section pattern with useState toggle)
    - frontend/src/api.ts (InsightsPayload, RequirementEntryPayload, PhaseWavePayload interfaces just created)
    - frontend/src/context.tsx (useApp hook — provides activeProject, activeSegment, loading)
    - .planning/phases/12-enhanced-visibility/12-CONTEXT.md (D-01 through D-14 decisions)
    - .planning/phases/12-enhanced-visibility/12-RESEARCH.md (section 5 — frontend patterns for tab bar, color conventions, collapsible sections)
  </read_first>
  <action>
    Create frontend/src/pages/InsightsPage.tsx as a named export function component.

    Structure per D-01: single page with three tabs (Requirements, Waves, Archives) using local useState per D-02.

    Tab bar: horizontal pill buttons matching ShellLayout NavLink styling. Define a TABS const array with id and label for "requirements", "waves", "archives". Active tab gets bg-[#2a2d2e] text-[#cccccc], inactive gets text-[#858585] hover:bg-[#2a2d2e] hover:text-[#cccccc]. Tab bar has border-b border-[#474747] and mb-6 spacing.

    Data fetching: useState for InsightsPayload (initially null). useEffect that calls fetchInsights(activeSegment.planningPath) when activeSegment changes (not on tab change — fetch once, display from cache). Handle loading and error states.

    Requirements tab (VIS-01, VIS-04 per D-03, D-04, D-06):
    - Group requirements by category field
    - Render category headers as section dividers (text-xs uppercase text-[#858585] with mt-4 mb-2)
    - Render a table under each category with columns: ID, Description, Phase, Status
    - Row styling per D-04: rows where is_gap=true get bg-red-900/20 tint; rows where status is "Pending" get bg-amber-900/20 tint; rows where status is "Complete" get no special tint
    - Status badges: "Complete" gets bg-green-900/40 text-[#4ec994], "Pending" gets bg-yellow-900/40 text-yellow-400, gap gets bg-red-900/40 text-red-400 with label "Unmapped"
    - Table cells use border-[#474747] borders, text-sm, padding px-3 py-2

    Waves tab (VIS-02 per D-07, D-08):
    - If no multi-wave phases exist, show empty state message
    - For each phase in wave_phases: show phase number and title as a header
    - Below header, show plans grouped by wave: "Wave N:" followed by plan names joined with comma
    - Use card layout (rounded-md border border-[#474747] bg-[#1e1e1e] p-4) per existing page patterns

    Archives tab (VIS-03 per D-10, D-11):
    - Filter activeProject.milestones to only those with is_archived === true
    - If none, show empty state
    - Each archived milestone renders as a collapsible accordion item using the VerificationPage toggle pattern (useState boolean per item)
    - Collapsed: show milestone title and completion_date (or "date unknown" if null)
    - Expanded: show list of phases with number, title, and status badge
    - Use existing color conventions: complete phases get green badge, others get gray

    Standard page guard: if loading show "Loading...", if no activeProject show "Add scan roots..." message. Matching DriftPage pattern.
  </action>
  <verify>
    <automated>cd D:/gsd-monitor-py/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/InsightsPage.tsx exists with named export InsightsPage
    - Component uses useApp() for activeProject and activeSegment
    - Three tabs render: Requirements, Waves, Archives
    - Requirements tab groups by category with section headers
    - Gap rows (is_gap=true) have bg-red-900/20 row class
    - Pending rows have bg-amber-900/20 row class
    - Waves tab only renders phases from wave_phases (multi-wave only per D-08)
    - Archives tab filters milestones by is_archived field
    - Archives uses collapsible accordion pattern
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>InsightsPage renders three tabs with requirements traceability table (grouped by category, gap-highlighted), wave visualization (multi-wave phases only), and archived milestone browser (collapsible accordions).</done>
</task>

<task type="auto">
  <name>Task 3: Route and nav wiring</name>
  <files>frontend/src/App.tsx, frontend/src/ShellLayout.tsx</files>
  <read_first>
    - frontend/src/App.tsx (current route definitions and imports)
    - frontend/src/ShellLayout.tsx (current nav array)
  </read_first>
  <action>
    In frontend/src/App.tsx:
    - Add import: import { InsightsPage } from "./pages/InsightsPage"
    - Add route inside Routes, between the /verification and /settings routes: Route with path="/insights" and element={InsightsPage component}

    In frontend/src/ShellLayout.tsx:
    - Add entry to the nav array between "Verification" and "Settings" (per D-12, using label "Insights"): { to: "/insights", label: "Insights" }
  </action>
  <verify>
    <automated>cd D:/gsd-monitor-py/frontend && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx imports InsightsPage from "./pages/InsightsPage"
    - App.tsx has Route with path="/insights" element={InsightsPage}
    - ShellLayout.tsx nav array contains { to: "/insights", label: "Insights" } between Verification and Settings entries
    - Frontend builds without errors
  </acceptance_criteria>
  <done>Insights page is accessible via /insights route and appears in sidebar navigation between Verification and Settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> frontend | Insights data rendered via React; no dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | InsightsPage | accept | All data is project metadata visible to local user; desktop app |
| T-12-05 | Spoofing | fetchInsights | accept | Local loopback API; no auth needed for desktop single-user app |
</threat_model>

<verification>
- cd frontend && npx tsc --noEmit (TypeScript compiles clean)
- cd frontend && npm run build (Vite build succeeds)
- Visual: navigate to /insights in the app, see three tabs, requirements table renders, waves render for multi-wave phases, archives show shipped milestones
</verification>

<success_criteria>
- InsightsPage is reachable from nav sidebar
- Requirements tab displays all requirements grouped by category with gap highlighting
- Waves tab shows multi-wave phases with plan groupings
- Archives tab shows shipped milestones in collapsible accordions
- Frontend builds and type-checks cleanly
</success_criteria>

<output>
Create `.planning/phases/12-enhanced-visibility/12-02-SUMMARY.md` when done
</output>
