---
phase: 12-enhanced-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gsd_monitor/parsers/requirements_parser.py
  - gsd_monitor/models/core.py
  - gsd_monitor/parsers/gsd_core_roadmap.py
  - gsd_monitor/api/app.py
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04

must_haves:
  truths:
    - "GET /api/insights/{planning_path} returns requirements with category, phase mapping, status, and is_gap fields"
    - "GET /api/insights/{planning_path} returns wave_phases with only multi-wave phases, each listing plan names and wave numbers"
    - "Milestone model has is_archived and completion_date fields; archived milestones from details blocks have is_archived=True"
    - "Requirements with no traceability table entry have is_gap=True"
  artifacts:
    - path: "gsd_monitor/parsers/requirements_parser.py"
      provides: "RequirementsParser and RequirementEntry"
      exports: ["RequirementsParser", "RequirementEntry"]
    - path: "gsd_monitor/models/core.py"
      provides: "Updated Milestone with is_archived and completion_date"
      contains: "is_archived"
    - path: "gsd_monitor/api/app.py"
      provides: "GET /api/insights endpoint"
      contains: "/api/insights/"
  key_links:
    - from: "gsd_monitor/api/app.py"
      to: "gsd_monitor/parsers/requirements_parser.py"
      via: "insights endpoint calls RequirementsParser.parse()"
      pattern: "RequirementsParser\\.parse"
    - from: "gsd_monitor/parsers/gsd_core_roadmap.py"
      to: "gsd_monitor/models/core.py"
      via: "sets is_archived=True on archived Milestone objects"
      pattern: "is_archived.*True"
---

<objective>
Backend: Create RequirementsParser, add insights API endpoint, and extend Milestone model for archive visibility.

Purpose: Provide the data layer for Phase 12 frontend — requirements traceability (VIS-01, VIS-04), wave visualization (VIS-02), and milestone archive browsing (VIS-03). All four VIS requirements depend on backend data that does not yet exist.

Output: New RequirementsParser, updated Milestone model, new /api/insights endpoint returning requirements and wave data.
</objective>

<execution_context>
@C:/Users/mike/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/mike/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

## Artifacts this phase produces

### New files
- `gsd_monitor/parsers/requirements_parser.py` — `RequirementsParser` class, `RequirementEntry` model

### New symbols
- `RequirementEntry` (Pydantic BaseModel in requirements_parser.py): fields `id`, `category`, `description`, `is_checked`, `phase`, `status`, `is_gap`
- `RequirementsParser.parse(planning_dir: str) -> list[RequirementEntry]` (static method)
- `Milestone.is_archived` (bool field, default False)
- `Milestone.completion_date` (str | None field, default None)
- `insights()` async endpoint function in app.py
- `_extract_wave_data(planning_dir: str) -> list[dict]` helper in app.py

### New endpoints
- `GET /api/insights/{planning_path:path}` — returns `{"requirements": [...], "wave_phases": [...]}`

<tasks>

<task type="auto">
  <name>Task 1: RequirementsParser and Milestone model updates</name>
  <files>gsd_monitor/parsers/requirements_parser.py, gsd_monitor/models/core.py, gsd_monitor/parsers/gsd_core_roadmap.py</files>
  <read_first>
    - gsd_monitor/models/core.py (current Milestone model — see fields to extend)
    - gsd_monitor/parsers/gsd_core_roadmap.py (see _extract_archived_milestones — where to set is_archived=True and extract completion_date)
    - .planning/REQUIREMENTS.md (the file to be parsed — understand exact format of ### category headings, - [x] **ID**: description lines, and ## Traceability table)
    - gsd_monitor/parsers/plan_parser.py (parser pattern reference: static parse method returning ParseResult)
  </read_first>
  <action>
    Create gsd_monitor/parsers/requirements_parser.py with:

    RequirementEntry (Pydantic BaseModel): fields id (str), category (str), description (str), is_checked (bool), phase (str | None), status (str | None), is_gap (bool). Per D-03, D-05, D-06.

    RequirementsParser with static method parse(planning_dir: str) -> list[RequirementEntry]:
    - Read {planning_dir}/REQUIREMENTS.md. Return empty list if not found.
    - Three regex passes per D-05 research findings:
      1. Category headings: match lines like "### Detection and Parsing" to track current category (D-06)
      2. Requirement lines: match "- [x] **DETECT-01**: description text" capturing checkbox state, ID, description
      3. Traceability table rows under "## Traceability": match "| DETECT-01 | Phase 11 | Complete |" capturing ID, phase, status
    - Join: for each requirement line, look up its traceability row. Set is_gap=True when no traceability row exists for a requirement ID (VIS-04 per D-04).
    - Use from __future__ import annotations at top. Follow existing parser naming: _CATEGORY_HDR, _REQ_LINE, _TRACE_ROW as module-level compiled regexes.

    In gsd_monitor/models/core.py, add two fields to the Milestone class:
    - is_archived: bool = False
    - completion_date: str | None = None

    In gsd_monitor/parsers/gsd_core_roadmap.py, in the _extract_archived_milestones method:
    - Set is_archived=True on each Milestone created in that method (the archived list append near the bottom)
    - Add a regex _SHIPPED_DATE = re.compile(r"SHIPPED\s+([\d-]+)") at module level
    - After extracting summary_title, attempt to match _SHIPPED_DATE against it. If matched, pass completion_date=match.group(1) to the Milestone constructor.
    - Also in _parse_milestones where archived milestones are re-appended with renumbered number, preserve is_archived=True and completion_date from arch_ms.
  </action>
  <verify>
    <automated>cd D:/gsd-monitor-py && python -c "
from gsd_monitor.parsers.requirements_parser import RequirementsParser, RequirementEntry
reqs = RequirementsParser.parse('.planning')
assert len(reqs) > 0, 'No requirements parsed'
vis01 = [r for r in reqs if r.id == 'VIS-01']
assert len(vis01) == 1, 'VIS-01 not found'
assert vis01[0].category != '', 'Category empty'
assert vis01[0].phase is not None, 'Phase missing'
det01 = [r for r in reqs if r.id == 'DETECT-01']
assert len(det01) == 1
assert det01[0].is_checked == True
print(f'OK: {len(reqs)} requirements parsed')
# Check Milestone model
from gsd_monitor.models.core import Milestone
m = Milestone(is_archived=True, completion_date='2026-04-12')
assert m.is_archived == True
assert m.completion_date == '2026-04-12'
print('OK: Milestone model updated')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - gsd_monitor/parsers/requirements_parser.py exists with class RequirementsParser and class RequirementEntry
    - RequirementsParser.parse('.planning') returns a list with 21 RequirementEntry objects (matching REQUIREMENTS.md)
    - Each RequirementEntry has non-empty category field matching its section heading
    - VIS-01 through VIS-04 have phase="Phase 12" and status="Pending"
    - DETECT-01 has is_checked=True, phase="Phase 11", status="Complete"
    - Requirements with no traceability row have is_gap=True (currently none expected, but logic must exist)
    - Milestone model in core.py has is_archived: bool = False and completion_date: str | None = None
    - _extract_archived_milestones sets is_archived=True on returned Milestone objects
    - _SHIPPED_DATE regex extracts date from summary text like "SHIPPED 2026-04-12"
  </acceptance_criteria>
  <done>RequirementsParser parses all 21 requirements with correct categories, traceability mapping, and gap detection. Milestone model supports is_archived and completion_date fields. Archived milestones from GsdCoreRoadmapParser have is_archived=True.</done>
</task>

<task type="auto">
  <name>Task 2: Insights API endpoint with wave data extraction</name>
  <files>gsd_monitor/api/app.py</files>
  <read_first>
    - gsd_monitor/api/app.py (current endpoint patterns — especially quick_tasks endpoint for path parameter handling, and the create_app function structure)
    - gsd_monitor/parsers/requirements_parser.py (just created in Task 1 — import path and parse signature)
    - gsd_monitor/parsers/plan_parser.py (understand current PlanParser — it does NOT read frontmatter; wave extraction must be done independently)
  </read_first>
  <action>
    Add a new endpoint inside create_app() in gsd_monitor/api/app.py, placed after the quick_tasks endpoint and before the docs_tree endpoint.

    Per D-13 (path of least resistance), create a new on-demand endpoint following the quick-tasks pattern exactly:

    GET /api/insights/{planning_path:path}

    The handler function insights(planning_path: str) should:
    1. URL-decode planning_path (from urllib.parse import unquote)
    2. Call RequirementsParser.parse(decoded_path) to get requirements list
    3. Call a helper _extract_wave_data(decoded_path) to get wave phases
    4. Return dict with "requirements" (list of model_dump dicts) and "wave_phases" (list of dicts)

    Add the import at the top of app.py: from gsd_monitor.parsers.requirements_parser import RequirementsParser

    Create helper function _extract_wave_data(planning_dir: str) -> list[dict] (defined outside create_app, near _group_to_json):
    - Resolve Path(planning_dir) / "phases"
    - If phases dir does not exist, return empty list
    - Iterate all subdirectories in phases/
    - For each subdir, glob for *-PLAN.md files
    - For each PLAN file, read the file and extract YAML frontmatter between --- delimiters
    - Parse wave: N from frontmatter using yaml-like line matching (re.search for r"^wave:\s*(\d+)", within the frontmatter block). Do NOT import yaml — use simple regex per existing codebase pattern.
    - Also extract plan number or use the filename as plan_name
    - Group results by phase directory name (which encodes phase number)
    - Extract phase number from directory name pattern (e.g., "11-gsd-core-support" -> 11)
    - Extract phase title from directory name (strip number prefix)
    - Filter: only include phases where plans span more than one distinct wave value (D-08)
    - Return list of dicts: each has phase_number (int), phase_title (str), plans (list of dicts with plan_name and wave)
    - Sort plans within each phase by wave number ascending
  </action>
  <verify>
    <automated>cd D:/gsd-monitor-py && python -c "
from gsd_monitor.api.app import _extract_wave_data
waves = _extract_wave_data('.planning')
print(f'Wave phases: {len(waves)}')
for wp in waves:
    print(f'  Phase {wp[\"phase_number\"]}: {len(wp[\"plans\"])} plans')
    for p in wp['plans']:
        print(f'    {p[\"plan_name\"]} wave={p[\"wave\"]}')
# Phase 11 has wave 1 and wave 2 plans, so should appear
assert any(wp['phase_number'] == 11 for wp in waves), 'Phase 11 (multi-wave) not found'
# Phase 9 has only 1 plan (wave 1), so should NOT appear
assert not any(wp['phase_number'] == 9 for wp in waves), 'Phase 9 (single-wave) should be filtered'
print('OK: wave data extraction working')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - gsd_monitor/api/app.py contains endpoint decorated with @application.get("/api/insights/{planning_path:path}")
    - The endpoint imports and calls RequirementsParser.parse()
    - _extract_wave_data function exists and reads PLAN.md frontmatter for wave field
    - Only phases with plans spanning multiple wave values are returned (D-08)
    - Phase 11 (which has wave 1 and wave 2 plans) appears in wave_phases
    - Phase 9 (single plan, single wave) does not appear
    - Response shape: {"requirements": [...], "wave_phases": [...]}
  </acceptance_criteria>
  <done>GET /api/insights/{planning_path} endpoint returns requirements traceability data and multi-wave phase data. Wave extraction reads PLAN.md frontmatter without importing yaml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | planning_path parameter from frontend is untrusted input |
| filesystem | REQUIREMENTS.md and PLAN.md files read from disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | /api/insights/{planning_path} | mitigate | URL-decode and resolve path; endpoint only reads .planning/ files, never writes; follows same pattern as existing quick-tasks and docs endpoints |
| T-12-02 | Information Disclosure | requirements_parser.py | accept | REQUIREMENTS.md content is project metadata, not secrets; app is local-only desktop |
| T-12-03 | Denial of Service | _extract_wave_data | accept | Iterates local filesystem dirs; bounded by number of phase directories; desktop app with single user |
</threat_model>

<verification>
- python -c "from gsd_monitor.parsers.requirements_parser import RequirementsParser; r = RequirementsParser.parse('.planning'); assert len(r) == 21"
- python -c "from gsd_monitor.api.app import _extract_wave_data; w = _extract_wave_data('.planning'); assert len(w) > 0"
- python -c "from gsd_monitor.models.core import Milestone; m = Milestone(is_archived=True); assert m.is_archived"
</verification>

<success_criteria>
- RequirementsParser extracts all requirements from REQUIREMENTS.md with categories and traceability mappings
- Insights endpoint returns structured JSON with requirements and wave data
- Milestone model supports is_archived and completion_date for archive browsing
- Archived milestones from GsdCoreRoadmapParser have is_archived=True
</success_criteria>

<output>
Create `.planning/phases/12-enhanced-visibility/12-01-SUMMARY.md` when done
</output>
