---
phase: 11-gsd-core-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gsd_monitor/models/enums.py
  - gsd_monitor/models/core.py
  - gsd_monitor/parsers/gsd_core_roadmap.py
  - gsd_monitor/parsers/state_parser.py
  - gsd_monitor/services/project_discovery.py
  - gsd_monitor/parsers/gsd2_roadmap.py
  - gsd_monitor/api/app.py
  - tests/test_gsd_core_roadmap_parser.py
  - tests/test_state_parser_progress.py
autonomous: true
requirements:
  - DETECT-01
  - DETECT-02
  - DETECT-03
  - DETECT-04
  - DETECT-05
  - DETECT-06
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06
  - DOCS-07
  - DOCS-08
  - PROG-01
  - PROG-02
  - PROG-03

must_haves:
  truths:
    - "A project with .planning/config.json is detected as GsdVersion.CORE"
    - "A gsd-core ROADMAP.md with heading-based phases (## Phase N: Title) is parsed into PhaseEntry list with goals"
    - "Milestone-prefixed phase IDs (Phase 1-01) are preserved in PhaseEntry.code"
    - "Emoji milestone markers are parsed into Milestone objects with correct status"
    - "Legacy GSD-1 checkbox ROADMAP format continues to parse correctly"
    - "No GSD-2 (.gsd/) projects appear in discovery results"
    - "STATE.md progress fields (total_phases, completed_phases, progress_percent) are extracted for all 3 syntax variants"
    - "New phase doc types (has_requirements, has_verification, has_summary, has_ui_spec, has_ui_review) are detected during enrichment"
    - "HANDOFF.json pause data (phase, plan, timestamp) is surfaced in project payload"
    - ".continue-here.md presence is detected and flagged"
    - "config.json fields (workflow mode, model profile, branching strategy) are surfaced in project payload"
  artifacts:
    - path: "gsd_monitor/parsers/gsd_core_roadmap.py"
      provides: "GsdCoreRoadmapParser class for heading-based ROADMAP.md"
      exports: ["GsdCoreRoadmapParser"]
    - path: "gsd_monitor/models/enums.py"
      provides: "GsdVersion.CORE enum value"
      contains: "CORE"
    - path: "gsd_monitor/models/core.py"
      provides: "Extended PhaseEntry and GsdProject with new fields"
      contains: "has_ui_spec"
    - path: "gsd_monitor/services/project_discovery.py"
      provides: "gsd-core detection, enrichment, GSD-2 removal"
    - path: "tests/test_gsd_core_roadmap_parser.py"
      provides: "Parser tests for heading-based format"
    - path: "tests/test_state_parser_progress.py"
      provides: "StateParser progress extraction tests"
  key_links:
    - from: "gsd_monitor/services/project_discovery.py"
      to: "gsd_monitor/parsers/gsd_core_roadmap.py"
      via: "import and call GsdCoreRoadmapParser.parse() in _build_gsd1_segment"
      pattern: "GsdCoreRoadmapParser\\.parse"
    - from: "gsd_monitor/services/project_discovery.py"
      to: "gsd_monitor/models/core.py"
      via: "uses new PhaseEntry fields and GsdProject fields during enrichment"
      pattern: "has_ui_spec|has_summary|handoff_info"
    - from: "gsd_monitor/api/app.py"
      to: "gsd_monitor/models/core.py"
      via: "serializes new fields to JSON via model_dump_json"
---

<objective>
Implement all backend changes for gsd-core support: new parser, model extensions, detection logic, enrichment for new doc types, GSD-2 removal, STATE.md progress extraction, HANDOFF.json/config.json parsing.

Purpose: The backend must correctly detect, parse, and enrich gsd-core projects so the API response contains all data the frontend needs for Phase 11 UI (per D-01, D-02, D-04, D-06, D-08, D-10).

Output: Working backend that serves gsd-core project data with all new fields, with GSD-2 code removed, verified by tests and API response inspection.
</objective>

<execution_context>
@D:/gsd-monitor-py/.claude/gsd-core/workflows/execute-plan.md
@D:/gsd-monitor-py/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-gsd-core-support/11-CONTEXT.md
</context>

## Artifacts this phase produces

**New files:**
- `gsd_monitor/parsers/gsd_core_roadmap.py` — `GsdCoreRoadmapParser` class with `parse(file_path: str) -> ParseResult` static method
- `tests/test_gsd_core_roadmap_parser.py` — Test suite for gsd-core ROADMAP.md parsing
- `tests/test_state_parser_progress.py` — Test suite for STATE.md progress extraction

**New enum values:**
- `GsdVersion.CORE = "gsd-core"` in `gsd_monitor/models/enums.py`

**New model fields on `PhaseEntry`:**
- `has_ui_spec: bool = False`
- `has_ui_review: bool = False`
- `has_summary: bool = False`
- `has_requirements: bool = False` (project-level, but tracked per phase for quick-access)

**New model fields on `GsdProject`:**
- `handoff_info: dict | None = None` — parsed HANDOFF.json (phase, plan, timestamp, paused flag)
- `continue_here: bool = False` — whether .continue-here.md exists
- `config_info: dict | None = None` — parsed config.json summary (workflow mode, model_profile, branching_strategy)
- `progress_percent: int = 0` — from STATE.md
- `completed_phases: int = 0` — from STATE.md
- `total_phases: int = 0` — from STATE.md

**New model fields on `StateInfo`:**
- `total_phases: int = 0`
- `completed_phases: int = 0`
- `progress_percent: int = 0`

**Deleted files:**
- `gsd_monitor/parsers/gsd2_roadmap.py` (deleted)

**Deleted enum values:**
- `GsdVersion.V2` removed

**Deleted methods on `ProjectDiscoveryService`:**
- `_discover_gsd2`
- `_enrich_gsd2_project`
- `_enrich_gsd2_slice`

**Deleted imports:**
- `Gsd2RoadmapParser` import removed from `project_discovery.py`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Models, GsdCoreRoadmapParser, StateParser progress extraction</name>
  <files>
    gsd_monitor/models/enums.py,
    gsd_monitor/models/core.py,
    gsd_monitor/parsers/gsd_core_roadmap.py,
    gsd_monitor/parsers/state_parser.py,
    tests/test_gsd_core_roadmap_parser.py,
    tests/test_state_parser_progress.py
  </files>
  <read_first>
    gsd_monitor/models/enums.py,
    gsd_monitor/models/core.py,
    gsd_monitor/parsers/roadmap.py,
    gsd_monitor/parsers/state_parser.py,
    .planning/phases/11-gsd-core-support/11-CONTEXT.md
  </read_first>
  <behavior>
    - GsdCoreRoadmapParser.parse on a heading-based ROADMAP returns PhaseEntry list with number, title, goal, status
    - Parser handles "- [x] **Phase N:" checkbox lines within ## Phase Details sections for plan completion
    - Parser extracts milestone sections from "## vX.Y" headings with emoji status markers
    - Parser handles milestone-prefixed phase IDs like "Phase 1-01" storing them in PhaseEntry.code
    - Parser handles archived milestones in details/summary blocks
    - StateParser extracts total_phases, completed_phases, progress_percent from YAML frontmatter
    - StateParser extracts progress from bold-inline format ("**total_phases:** 8")
    - StateParser extracts progress from pipe-table format ("| total_phases | 8 |")
  </behavior>
  <action>
    Add GsdVersion.CORE = "gsd-core" to enums.py (per D-01 detection).

    Add new fields to PhaseEntry: has_ui_spec (bool, default False), has_ui_review (bool, default False), has_summary (bool, default False), has_requirements (bool, default False).

    Add new fields to GsdProject: handoff_info (dict or None, default None), continue_here (bool, default False), config_info (dict or None, default None), progress_percent (int, default 0), completed_phases (int, default 0), total_phases (int, default 0).

    Add new fields to StateInfo: total_phases (int, default 0), completed_phases (int, default 0), progress_percent (int, default 0).

    Create gsd_monitor/parsers/gsd_core_roadmap.py with GsdCoreRoadmapParser class following the static parse(file_path) -> ParseResult pattern from RoadmapParser (per D-01). The parser must handle:
    - Heading-based phase format: lines matching "## Phase N: Title" or "### Phase N: Title" under "## Phase Details" (per DETECT-02)
    - Goal extraction: "**Goal**:" or "**Goal:**" lines within phase sections
    - Phase status from plan completion lines like "**Plans:** N/M plans complete" and "- [x]"/"- [ ]" plan checkboxes
    - Milestone extraction from "## vX.Y" headings. Emoji markers: checkmark or (completed) emoji = COMPLETED, rocket or similar = ACTIVE, else PLANNED (per DETECT-03)
    - Milestone-prefixed phase IDs: when a phase line reads "Phase 1-01: Title", store "1-01" in PhaseEntry.code and parse the numeric part for PhaseEntry.number (per DETECT-04, D-03)
    - Archived milestones inside HTML details/summary blocks
    - The parser returns a GsdProject with name extracted from "# Roadmap: Name" heading, milestones list, and version=GsdVersion.CORE

    Enhance StateParser.parse to extract progress fields from STATE.md (per PROG-01, PROG-03, D-09):
    - YAML frontmatter: look for total_phases, completed_phases, percent under "progress:" key
    - Bold inline: lines like "**total_phases:** 8" or "**completed_phases:** 5"
    - Pipe-table: rows like "| total_phases | 8 |" or "| completed_phases | 5 |"
    - Store extracted values in StateInfo.total_phases, StateInfo.completed_phases, StateInfo.progress_percent

    Write tests in tests/test_gsd_core_roadmap_parser.py covering: basic heading parse, milestone extraction with emoji, milestone-prefixed IDs, goal extraction, plan status extraction, archived milestones.

    Write tests in tests/test_state_parser_progress.py covering: YAML frontmatter progress, bold-inline progress, pipe-table progress, missing progress fields.
  </action>
  <verify>
    <automated>cd D:/gsd-monitor-py && python -m pytest tests/test_gsd_core_roadmap_parser.py tests/test_state_parser_progress.py tests/test_roadmap_parser.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - GsdVersion enum has exactly three values: V1="gsd1", CORE="gsd-core" (no V2)
    - PhaseEntry model has fields has_ui_spec, has_ui_review, has_summary, has_requirements all defaulting to False
    - GsdProject model has fields handoff_info, continue_here, config_info, progress_percent, completed_phases, total_phases
    - StateInfo model has fields total_phases, completed_phases, progress_percent all defaulting to 0
    - GsdCoreRoadmapParser.parse returns ParseResult.ok with GsdProject containing milestones and phases for valid input
    - GsdCoreRoadmapParser correctly stores "1-01" in PhaseEntry.code for "Phase 1-01: Title"
    - StateParser.parse extracts progress_percent=50 from a STATE.md with "percent: 50" in YAML frontmatter
    - Existing tests/test_roadmap_parser.py still passes (legacy GSD-1 format, per DETECT-05)
  </acceptance_criteria>
  <done>New parser parses gsd-core ROADMAP.md format. StateParser extracts progress. Models have all new fields. All tests pass including legacy roadmap parser tests.</done>
</task>

<task type="auto">
  <name>Task 2: Discovery refactor — gsd-core detection, enrichment, GSD-2 removal</name>
  <files>
    gsd_monitor/services/project_discovery.py,
    gsd_monitor/parsers/gsd2_roadmap.py,
    gsd_monitor/api/app.py
  </files>
  <read_first>
    gsd_monitor/services/project_discovery.py,
    gsd_monitor/parsers/gsd2_roadmap.py,
    gsd_monitor/api/app.py,
    gsd_monitor/models/core.py,
    gsd_monitor/parsers/gsd_core_roadmap.py
  </read_first>
  <action>
    GSD-2 REMOVAL (per D-10): Delete the file gsd_monitor/parsers/gsd2_roadmap.py entirely. In project_discovery.py, remove: the import of Gsd2RoadmapParser, the _discover_gsd2 method, the _enrich_gsd2_project method, the _enrich_gsd2_slice method. In discover_groups, remove the entire first for-loop that scans for ".gsd" directories (lines scanning _find_dirs for ".gsd"), remove the gsd2_repos set and all references to it (the "if canon_key in gsd2_repos: continue" check). Remove the GsdVersion.V2 enum value from enums.py (already done in Task 1 but verify).

    GSD-CORE DETECTION (per D-02, DETECT-01): In _build_gsd1_segment, after determining the planning base, check if config.json exists at base / "config.json". If it does, set version to GsdVersion.CORE and use GsdCoreRoadmapParser.parse instead of RoadmapParser.parse for the roadmap. As fallback (per D-02), if config.json is absent but the ROADMAP.md contains heading-based phase format (look for "## Phase" or "### Phase" pattern in first 2000 chars), also use GsdCoreRoadmapParser. Import GsdCoreRoadmapParser from gsd_monitor.parsers.gsd_core_roadmap.

    ENRICHMENT FOR NEW DOC TYPES: In _enrich_phase, after the existing has_context/has_research/has_plan/has_uat/has_validation detection block, add detection for:
    - has_ui_spec: check for {padded}-UI-SPEC.md in phase_dir
    - has_ui_review: check for {padded}-UI-REVIEW.md in phase_dir
    - has_summary: check if summary_files list (already computed) is non-empty
    - has_requirements: check if REQUIREMENTS.md exists in planning_dir (project-level doc)
    Include these new fields in the PhaseEntry constructor call at the end of _enrich_phase.

    HANDOFF.JSON PARSING (per DOCS-06, D-06): In _build_gsd1_segment, after building the project, check for base / "HANDOFF.json". If it exists, read and parse it as JSON. Extract: the "phase" key (string), "plan" key (string), "timestamp" key (string), and set "paused" to True. Store as dict in project.handoff_info via model_copy.

    .CONTINUE-HERE.MD (per DOCS-07): In _build_gsd1_segment, check for base / ".continue-here.md". If it exists, set project.continue_here = True via model_copy.

    CONFIG.JSON SURFACING (per DOCS-08, D-07): In _build_gsd1_segment, if config.json was read for detection, also extract a summary dict with keys: workflow_mode (from config "mode" or config["workflow"]["discuss_mode"]), model_profile (from config "model_profile"), branching_strategy (from config["git"]["branching_strategy"]). Store in project.config_info via model_copy.

    PROGRESS METRICS (per PROG-01, PROG-02): In _build_gsd1_segment, where StateParser is already called, use the new StateInfo progress fields. Set project.progress_percent, project.completed_phases, project.total_phases from the StateInfo values via model_copy.

    API RESPONSE: The _segment_to_json function in app.py already serializes via model_dump_json, so new fields on GsdProject and PhaseEntry will automatically appear in the JSON response. No API changes needed beyond verifying the serialization includes new fields. However, remove the Gsd2RoadmapParser import if it was imported in app.py (it is not currently imported there, but verify).
  </action>
  <verify>
    <automated>cd D:/gsd-monitor-py && python -m pytest tests/ -x -v --ignore=tests/test_gsd2_stateparser.py</automated>
  </verify>
  <acceptance_criteria>
    - gsd_monitor/parsers/gsd2_roadmap.py file no longer exists
    - project_discovery.py has no import of Gsd2RoadmapParser
    - project_discovery.py has no method named _discover_gsd2
    - project_discovery.py has no method named _enrich_gsd2_project
    - project_discovery.py has no method named _enrich_gsd2_slice
    - discover_groups does not scan for ".gsd" directories
    - _build_gsd1_segment checks for config.json and uses GsdCoreRoadmapParser when present
    - _enrich_phase sets has_ui_spec=True when {padded}-UI-SPEC.md exists
    - _enrich_phase sets has_ui_review=True when {padded}-UI-REVIEW.md exists
    - _enrich_phase sets has_summary=True when summary files exist
    - _build_gsd1_segment reads HANDOFF.json and populates handoff_info dict
    - _build_gsd1_segment detects .continue-here.md and sets continue_here=True
    - _build_gsd1_segment reads config.json and populates config_info dict
    - _build_gsd1_segment populates progress_percent, completed_phases, total_phases from StateParser
    - All existing tests pass (except gsd2-specific tests which are now irrelevant)
    - python -c "from gsd_monitor.services.project_discovery import ProjectDiscoveryService" succeeds
  </acceptance_criteria>
  <done>Backend fully supports gsd-core detection, all new doc types enriched, GSD-2 code removed, HANDOFF/config/progress data surfaced. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Remove GSD-2 test file and verify clean import</name>
  <files>
    tests/test_gsd2_stateparser.py
  </files>
  <read_first>
    tests/test_gsd2_stateparser.py
  </read_first>
  <action>
    Delete tests/test_gsd2_stateparser.py since it tests GSD-2 StateParser patterns that are no longer relevant after GSD-2 removal (per D-10).

    Run full test suite to confirm nothing references the deleted GSD-2 parser or enum.
  </action>
  <verify>
    <automated>cd D:/gsd-monitor-py && python -m pytest tests/ -x -v && python -c "from gsd_monitor.models.enums import GsdVersion; assert not hasattr(GsdVersion, 'V2'); assert GsdVersion.CORE.value == 'gsd-core'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_gsd2_stateparser.py no longer exists
    - python -m pytest tests/ -x passes with zero failures
    - GsdVersion enum has no V2 member
    - GsdVersion.CORE.value equals "gsd-core"
  </acceptance_criteria>
  <done>All GSD-2 artifacts removed. Full test suite green. Clean imports confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem -> Parser | Untrusted markdown/JSON files from scanned projects |
| API -> Frontend | JSON responses consumed by React UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | HANDOFF.json parsing | mitigate | Use json.loads with try/except; validate expected keys exist before accessing; do not eval or exec |
| T-11-02 | Tampering | config.json parsing | mitigate | Use json.loads with try/except; extract only known keys with .get() defaults |
| T-11-03 | Information Disclosure | config.json surfacing | accept | config.json contains workflow config, not secrets; app is local-only |
| T-11-04 | Denial of Service | Large ROADMAP.md parsing | accept | Local app scanning local files; user controls scan roots |
| T-11-05 | Tampering | Path traversal via planning_path | mitigate | Existing path traversal guard in docs_file endpoint unchanged |
</threat_model>

<verification>
1. Run full test suite: `python -m pytest tests/ -x -v`
2. Start app and verify gsd-core project appears with correct phases
3. Verify GSD-2 projects no longer appear
4. Verify API response contains new fields: handoff_info, config_info, progress_percent, has_ui_spec, etc.
</verification>

<success_criteria>
- All 17 requirements have backend support (detection, parsing, enrichment, API exposure)
- GSD-2 code fully removed
- All tests pass
- API serves correct data for gsd-core projects
</success_criteria>

<output>
Create `.planning/phases/11-gsd-core-support/11-01-SUMMARY.md` when done
</output>
