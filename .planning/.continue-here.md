---
context: default
phase: ad-hoc debug — stale docs / live refresh
task: 11
total_tasks: 13
status: blocked
last_updated: "2026-07-05T23:23:20.534Z"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: POST /api/groups/refresh returns stale data — A full forced rescan still returns stale project metadata on the test PC. Do NOT assume the watcher or WebSocket are the root cause. The scan itself is suspect. Investigate discover_groups() path resolution FIRST before touching any other code.

**Do not proceed until all boxes are checked.**

<current_state>
Ad-hoc debug session for the stale docs / live refresh bug in GSD Monitor.

The user has a test PC running GSD Monitor. Even after:
- Clicking the ↻ refresh button (which now calls POST /api/groups/refresh = full disk rescan)
- Restarting the app
- Confirming scan roots are correct in Settings
- Confirming project files are up to date on disk

...the app still shows stale project data (old phases, old doc content).

This means discover_groups() is either: reading from the wrong paths, or parsing correctly but returning stale output for some other reason.

All infrastructure fixes (WebSocket reconnect, watcher health check, installer, auto-update) are done and committed. The remaining issue is in the scan/discovery layer.
</current_state>

<completed_work>

- WebSocket exponential-backoff reconnect added to context.tsx (6f2f12a)
- Watchdog Observer 15s health-check timer added to file_watcher.py (6f2f12a)
- reload() on WebSocket onopen to close startup race (51eb9e5)
- start.bat auto-update: git fetch + reset --hard + pip + npm build (81fe314)
- Installer: git fetch --quiet to suppress stderr (dcf469f)
- Installer: git reset --hard instead of git pull (4a22811)
- start.bat: git reset --hard instead of git pull (4f9d0b8)
- start.bat: explicit error messages replacing all silent failures (f3366dc)
- Shell header: ↻ manual refresh button added (89875de)
- Refresh button: fixed to call POST /api/groups/refresh not GET (a73f0c5)
</completed_work>

<remaining_work>

- ROOT CAUSE: find why POST /api/groups/refresh returns stale data after forced rescan
- Trace the full path: scan_root → discover_groups() → planningPath → file fetch
- Check if discover_groups() is reading from correct .planning/ paths on test PC
- Fix whatever is causing the scan to return wrong/stale data
</remaining_work>

<decisions_made>

- git reset --hard instead of git pull: avoids merge conflicts when tracked dist files differ locally
- Refresh button calls POST not GET: GET only returns cached state.groups, POST triggers actual rescan
- forceRefresh() added to context alongside reload(): reload() stays for WebSocket-triggered updates (lightweight), forceRefresh() for manual button (triggers real scan)
</decisions_made>

<blockers>

- CRITICAL: POST /api/groups/refresh still returns stale data. The scan is running but returning wrong results. Root cause unknown — needs path tracing through discover_groups().
- Human action needed: on test PC, open browser devtools (if possible via pywebview) or add a /api/debug endpoint to see the raw planningPath values being returned by the server. This is the fastest way to confirm whether the paths are right.
</blockers>

## Required Reading (in order)

1. `gsd_monitor/services/project_discovery.py` — full file, understand how scan_roots → groups → planningPath is constructed
2. `gsd_monitor/services/planning_layout.py` — how iter_planning_contexts finds .planning/ dirs
3. `gsd_monitor/api/app.py` lines 83-91 — refresh() method, confirm scan_roots source
4. `.planning/HANDOFF.json` — structured state

## Critical Anti-Patterns (do NOT repeat these)

- DO NOT keep adding frontend fixes (WebSocket, reconnect, refresh button) without first confirming the backend scan is working correctly. All the frontend machinery is now correct. The bug is in the backend scan.
- DO NOT assume the watcher is the problem — the forced rescan (POST) also returns stale data, which rules out the watcher entirely.

## Infrastructure State

- All commits pushed to origin/master
- Test PC auto-updates on start.bat launch
- Frontend dist is current and committed
- No uncommitted changes

## Next Diagnostic Step

Add a `/api/debug/scan` endpoint to app.py that returns:
```json
{
  "scan_roots": [...],
  "groups": [{"id": "...", "root_path": "...", "planning_path": "..."}]
}
```
This will show the exact paths the server is scanning vs what the user expects.

OR: Read project_discovery.py fully and trace how a scan_root like `D:\projects` becomes a planningPath. The bug is likely in path resolution (Windows backslash handling, symlinks, or case sensitivity).

<context>
The core mystery: a full rescan still returns stale data. All watcher/WebSocket fixes are done. The issue is in discover_groups() itself — either it's finding the wrong .planning/ directories, or the parsed data from those directories is old for some reason. Most likely hypothesis: Windows path normalization issue causing it to scan a different location than the configured scan root.
</context>

<next_action>
Start with: Read gsd_monitor/services/project_discovery.py (full file) and gsd_monitor/services/planning_layout.py to trace the exact path from scan_root to planningPath. Look specifically for Windows path normalization, symlink resolution, or any place where a path might resolve to a different location than expected.
</next_action>
