---
phase: 17-installation-upgrade-scripts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - install.ps1
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03
  - INST-04
  - INST-05

must_haves:
  truths:
    - "Running install.ps1 on clean machine creates desktop shortcut and working start.bat"
    - "pip install -e . in fresh venv installs all required packages"
    - "Running install.ps1 a second time on existing install completes without corruption (existing upgrade-detection block)"
    - "Missing prereqs (Git, Python 3.11+, Node.js) produce human-readable Fail() error (existing prereq checks)"
    - "WebView2 absence prints download link (existing wv2OK check)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Hatchling-based package definition enabling pip install -e ."
      contains: "requires-python = '>= 3.11'"
    - path: "install.ps1"
      provides: "Updated installer using pip install -e . instead of listing packages"
      contains: "pip install -e"
  key_links:
    - "pyproject.toml dependencies must match the 7 packages previously listed inline in install.ps1"
    - "start.bat template inside install.ps1 must also use pip install -e for its auto-update section"
---

<objective>
Create pyproject.toml and update install.ps1 to use pip install -e . for dependency management.

Purpose: Centralizes dependency declarations in pyproject.toml so install.ps1, start.bat auto-updater, and future upgrade.ps1 all reference one source of truth instead of duplicating package lists. Satisfies INST-01 through INST-05 (prereq checks, clone, venv, build, launcher, shortcut, idempotent re-run already exist — this plan upgrades the pip strategy).

Output: pyproject.toml at project root; updated install.ps1 using pip install -e .
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@install.ps1
@gsd_monitor/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 0: Audit existing install.ps1 — confirm pre-existing features satisfy INST-02 through INST-05</name>
  <files></files>
  <action>
Read install.ps1 and confirm the following existing features are present (these must NOT be removed or changed — only the pip install lines are being updated in Tasks 1-2):

- INST-02: WebView2 check (grep "WebView2" or "wv2") → should find registry check + download link message
- INST-03: Prereq checks for Git, Python 3.11+, Node.js with Fail() calls naming each missing tool
- INST-04: Upgrade detection block (grep "Test-Path.*\.git") → checks for existing .git dir and runs upgrade path
- INST-05: start.bat creation block (grep "Set-Content.*start\.bat") → writes start.bat to install dir

If any feature is missing, add it following the existing style (Write-Step / Write-OK / Fail pattern).
If all are present, log findings and continue to Task 1.
  </action>
  <verify>
    <automated>cd /d/gsd-monitor-py && grep -q "wv2OK" install.ps1 && grep -q "developer.microsoft.com" install.ps1 && grep -q "Test-Path.*\.git" install.ps1 && grep -q "start\.bat" install.ps1 && echo "OK: INST-02/04/05 confirmed present"</automated>
  </verify>
  <done>Confirmed install.ps1 already satisfies INST-02 (WebView2), INST-03 (prereq errors), INST-04 (idempotent upgrade), INST-05 (start.bat launcher)</done>
</task>

<task type="auto">
  <name>Task 1: Create pyproject.toml with hatchling build backend</name>
  <files>pyproject.toml</files>
  <action>
Create pyproject.toml at project root with:

[build-system] section: requires hatchling, build-backend = "hatchling.build"

[project] section:
- name = "gsd-monitor"
- dynamic = ["version"] (read from gsd_monitor/__init__.py)
- description = "Desktop companion for Get Shit Done projects"
- requires-python = ">= 3.11"
- dependencies listing all 7 runtime packages with minimum bounds:
  fastapi>=0.115.0, uvicorn[standard]>=0.32.0, pydantic>=2.10.0, pydantic-settings>=2.6.0, pywebview>=5.3, pygit2>=1.14.0, watchdog>=6.0.0

[tool.hatch.version] section: path = "gsd_monitor/__init__.py"

[tool.hatch.build.targets.wheel] section: packages = ["gsd_monitor"]

No optional-dependencies or dev dependencies — keep minimal.
  </action>
  <verify>
    <automated>cd /d/gsd-monitor-py && python -c "import tomllib; d=tomllib.open(open('pyproject.toml','rb')); assert d['project']['requires-python']=='>= 3.11'; assert len(d['project']['dependencies'])==7; print('OK')" 2>/dev/null || python -c "import tomllib; f=open('pyproject.toml','rb'); d=tomllib.load(f); assert d['project']['requires-python']=='>= 3.11'; assert len(d['project']['dependencies'])==7; print('OK')"</automated>
  </verify>
  <done>pyproject.toml exists with correct build-system, all 7 dependencies, requires-python constraint, and hatch version pointing to gsd_monitor/__init__.py</done>
</task>

<task type="auto">
  <name>Task 2: Update install.ps1 to use pip install -e . throughout</name>
  <files>install.ps1</files>
  <action>
Three edits to install.ps1:

1. Fresh install section (Section 4, around line 210): Replace the multi-line pip install command that lists individual packages with:
   pip install -e "$installDir"
   Keep the same error handling pattern (check LASTEXITCODE, call Fail if non-zero).

2. Upgrade section (Section 3, around line 175): Replace the multi-line pip install command with:
   pip install -e "$installDir"
   Keep existing error handling.

3. start.bat template (Section 5, around line 266): Replace the line that runs:
   .venv\Scripts\pip.exe install -q fastapi "uvicorn[standard]" pydantic pydantic-settings pywebview pygit2 watchdog
   with:
   .venv\Scripts\pip.exe install -q -e "%~dp0."
   The "%~dp0." resolves to the directory containing start.bat (the install dir), with trailing dot for pip -e syntax.

Do NOT change any other logic — prereq checks, path resolution, git operations, shortcut creation all remain as-is.
  </action>
  <verify>
    <automated>cd /d/gsd-monitor-py && grep -c "pip.*install.*-e" install.ps1 | grep -q "3" && echo "OK: 3 pip install -e references found"</automated>
  </verify>
  <done>install.ps1 contains exactly 3 pip install -e invocations (fresh install, upgrade, start.bat template) with zero inline package lists remaining</done>
</task>

<task type="auto">
  <name>Task 3: Verify pip install -e . works in current environment</name>
  <files></files>
  <action>
Run pip install -e . from the project root using the existing .venv to confirm pyproject.toml is valid and all dependencies resolve. This is the smoke test that proves the pyproject.toml is correct before committing.

If pip install -e . fails, fix pyproject.toml (likely a syntax issue or missing field) and retry.
  </action>
  <verify>
    <automated>cd /d/gsd-monitor-py && .venv/Scripts/pip.exe install -e . --dry-run 2>&1 | head -5</automated>
  </verify>
  <done>pip install -e . completes successfully (or dry-run shows it would succeed) — pyproject.toml is valid</done>
</task>


<task type="checkpoint:commit">
  <name>Task 4: Commit</name>
  <action>
git add pyproject.toml install.ps1
git commit -m "feat(install): add pyproject.toml, upgrade install.ps1 to pip install -e . (INST-01-05)

Centralizes dependency declarations in pyproject.toml so install.ps1,
start.bat auto-updater, and upgrade.ps1 all use pip install -e . instead
of maintaining a duplicate inline package list.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
  </action>
  <done>Changes committed to git</done>
</task>

<task type="checkpoint:write-summary">
  <name>Task 5: Write SUMMARY</name>
  <action>
Create `.planning/phases/17-installation-upgrade-scripts/17-01-SUMMARY.md`:

```markdown
---
plan: 17-01
status: complete
---

# Summary: Plan 17-01 — install.ps1 + pyproject.toml

## What was done
- Created pyproject.toml with hatchling build backend, all 7 runtime deps, requires-python >= 3.11
- Updated install.ps1 fresh-install section to use pip install -e "$installDir"
- Updated install.ps1 upgrade section to use pip install -e "$installDir"
- Updated start.bat template inside install.ps1 to use pip install -q -e "%~dp0."
- Verified pip install -e . works in existing .venv

## Key decisions
- pyproject.toml uses dynamic version read from gsd_monitor/__init__.py via hatch
- No dev dependencies in pyproject.toml — kept minimal for installer use case
- start.bat template uses "%~dp0." (trailing dot) to resolve correctly from the installed dir
```
  </action>
  <done>17-01-SUMMARY.md written</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.ps1 input | InstallPath parameter from user — already validated for UNC and .. |
| pip install | Fetches packages from PyPI — trusted upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | pyproject.toml deps | low | accept | Dependencies use >= bounds matching existing install.ps1 behavior; no new risk introduced |
| T-17-02 | Tampering | pip install -e | low | accept | Editable install uses local source — same trust level as running python -m gsd_monitor directly |
</threat_model>

<verification>
- pyproject.toml parses without error via tomllib
- install.ps1 contains no remaining inline package lists (grep for "fastapi.*uvicorn.*pydantic" returns 0 matches)
- pip install -e . resolves all deps in dry-run mode
</verification>

<success_criteria>
pyproject.toml enables pip install -e . for the project. install.ps1 uses this in all three locations where it previously listed packages inline. The installer still satisfies INST-01 through INST-05 (prereq checks, clone, venv, pip install, frontend build, start.bat launcher, desktop shortcut, idempotent upgrade).
</success_criteria>

<output>
Create `.planning/phases/17-installation-upgrade-scripts/17-01-SUMMARY.md` when done
</output>
