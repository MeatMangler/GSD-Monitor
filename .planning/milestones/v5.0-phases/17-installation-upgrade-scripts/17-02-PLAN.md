---
phase: 17-installation-upgrade-scripts
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - upgrade.ps1
  - upgrade.bat
autonomous: true
requirements:
  - UPG-01
  - UPG-02
  - UPG-03

must_haves:
  truths:
    - "Running upgrade.ps1 from install dir pulls latest and rebuilds"
    - "Running upgrade.ps1 outside a valid install exits with clear error"
    - "Running upgrade.ps1 when already on latest prints up-to-date message and exits cleanly"
  artifacts:
    - path: "upgrade.ps1"
      provides: "Standalone upgrade script for existing installations"
      contains: "pip install"
    - path: "upgrade.bat"
      provides: "Batch launcher for upgrade.ps1"
      contains: "upgrade.ps1"
  key_links:
    - "upgrade.ps1 uses pip install -e from pyproject.toml created in plan 17-01"
    - "Valid-install guard checks for gsd_monitor dir and .venv\\Scripts\\pip.exe"
---

<objective>
Create standalone upgrade.ps1 and upgrade.bat scripts for existing GSD Monitor installations.

Purpose: Gives developers a deliberate, interactive-free upgrade path that is gentler than the start.bat auto-updater (git pull instead of git reset --hard). Satisfies UPG-01 (git pull + pip install + rebuild), UPG-02 (valid-install guard), UPG-03 (already-up-to-date detection).

Output: upgrade.ps1 and upgrade.bat at project root.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@install.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create upgrade.ps1 with valid-install guard and upgrade logic</name>
  <files>upgrade.ps1</files>
  <action>
Create upgrade.ps1 at project root. Structure:

Header: #Requires -Version 5.1, synopsis comment block explaining purpose.

Configuration:
- $ErrorActionPreference = 'Stop'
- [Console]::OutputEncoding = [System.Text.Encoding]::UTF8

Helper functions (same pattern as install.ps1): Write-Step, Write-OK, Fail.

Banner: "GSD Monitor Upgrade" in White.

Section 1 — Valid-install guard (UPG-02):
- $root = $PSScriptRoot
- Test-Path (Join-Path $root 'gsd_monitor') — if missing, Fail with message: "This script must be run from a valid GSD Monitor installation directory. Expected 'gsd_monitor' folder not found in: $root"
- Test-Path (Join-Path $root '.venv\Scripts\pip.exe') — if missing, Fail with message: "Virtual environment not found. Expected '.venv\Scripts\pip.exe' in: $root. Run install.ps1 first."

Section 2 — Fetch and compare (UPG-03):
- Write-Step "Checking for updates..."
- git -C $root fetch origin master --quiet
- If LASTEXITCODE ne 0, Fail "Could not reach remote repository. Check your internet connection."
- $localHash = git -C $root rev-parse HEAD
- $remoteHash = git -C $root rev-parse origin/master
- If $localHash equals $remoteHash: Write-Host message "Already up to date. Nothing to do." in Green, then exit 0.

Section 3 — Pull:
- Write-Step "Pulling latest changes..."
- git -C $root pull origin master
- If LASTEXITCODE ne 0, Fail "git pull failed. You may have local changes — commit or stash them first."
- Write-OK "Repository updated"

Section 4 — pip install:
- Write-Step "Updating Python dependencies..."
- Invoke .venv\Scripts\pip.exe install -e "$root"
- If LASTEXITCODE ne 0, Fail "pip install failed"
- Write-OK "Dependencies updated"

Section 5 — Frontend rebuild:
- Write-Step "Rebuilding frontend..."
- Push-Location (Join-Path $root 'frontend')
- npm ci
- If LASTEXITCODE ne 0, Pop-Location then Fail "npm ci failed"
- npm run build
- If LASTEXITCODE ne 0, Pop-Location then Fail "npm run build failed"
- Pop-Location
- Write-OK "Frontend rebuilt"

Section 6 — Success:
- Print success banner with "Upgrade complete. Run start.bat to launch." in Green.

No -NoWait parameter, no Read-Host pause — script is non-interactive by design.
  </action>
  <verify>
    <automated>cd /d/gsd-monitor-py && test -f upgrade.ps1 && grep -c "PSScriptRoot" upgrade.ps1 | grep -qE "^[5-9]|^[1-9][0-9]" && grep -q "Already up to date" upgrade.ps1 && grep -q "gsd_monitor" upgrade.ps1 && echo "OK"</automated>
  </verify>
  <done>upgrade.ps1 exists with valid-install guard checking gsd_monitor dir and .venv, fetch+compare for up-to-date detection, git pull, pip install -e, and npm ci + build</done>
</task>

<task type="auto">
  <name>Task 2: Create upgrade.bat launcher</name>
  <files>upgrade.bat</files>
  <action>
Create upgrade.bat at project root. Contents:

Line 1: @echo off
Line 2: powershell -ExecutionPolicy Bypass -File "%~dp0upgrade.ps1" %*

That is the entire file — no auto-update logic, no additional commands. The bat file is simply a convenience launcher for users who double-click rather than running PowerShell directly.

Use ASCII encoding (no BOM).
  </action>
  <verify>
    <automated>cd /d/gsd-monitor-py && test -f upgrade.bat && grep -q "upgrade.ps1" upgrade.bat && wc -l upgrade.bat | grep -qE "^\s*[23]\s" && echo "OK"</automated>
  </verify>
  <done>upgrade.bat exists as a 2-line launcher that invokes upgrade.ps1 with ExecutionPolicy Bypass</done>
</task>


<task type="checkpoint:commit">
  <name>Task 3: Commit</name>
  <action>
git add upgrade.ps1 upgrade.bat
git commit -m "feat(upgrade): add upgrade.ps1 and upgrade.bat (UPG-01-03)

Standalone upgrade script with valid-install guard, git pull, pip install -e .,
frontend rebuild, and already-up-to-date detection. upgrade.bat is a
convenience launcher that invokes upgrade.ps1 via PowerShell.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
  </action>
  <done>Changes committed to git</done>
</task>

<task type="checkpoint:write-summary">
  <name>Task 4: Write SUMMARY</name>
  <action>
Create `.planning/phases/17-installation-upgrade-scripts/17-02-SUMMARY.md`:

```markdown
---
plan: 17-02
status: complete
---

# Summary: Plan 17-02 — upgrade.ps1 + upgrade.bat

## What was done
- Created upgrade.ps1 with valid-install guard (checks gsd_monitor/ and .venv/Scripts/pip.exe)
- Fetch + hash comparison for already-up-to-date detection (exits 0 with clear message)
- git pull → pip install -e . → npm ci && npm run build upgrade sequence
- Created upgrade.bat as 2-line launcher: powershell -ExecutionPolicy Bypass -File upgrade.ps1

## Key decisions
- Uses git pull (not git reset --hard) — gentler than start.bat auto-updater
- Non-interactive by design — no Read-Host prompts
- PSScriptRoot anchors all paths so script works regardless of where it is invoked from
```
  </action>
  <done>17-02-SUMMARY.md written</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git pull | Fetches from configured origin remote — same trust as start.bat |
| npm ci | Installs from package-lock.json — deterministic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03 | Tampering | git pull | low | accept | Uses standard git pull from origin/master — same trust model as existing start.bat auto-updater |
| T-17-04 | Denial of Service | upgrade.ps1 network | low | mitigate | Fetch failure produces clear error message and exits without corrupting state |
</threat_model>

<verification>
- upgrade.ps1 exits with error when run from a directory without gsd_monitor/ folder
- upgrade.ps1 exits with "Already up to date" when local matches remote
- upgrade.bat invokes upgrade.ps1 correctly
</verification>

<success_criteria>
A developer with an existing GSD Monitor installation can double-click upgrade.bat (or run upgrade.ps1 directly) and the script will: validate it is in a proper install dir, check for updates, pull if needed, reinstall deps, rebuild frontend, and report success. Running when already current prints a clean message and exits 0.
</success_criteria>

<output>
Create `.planning/phases/17-installation-upgrade-scripts/17-02-SUMMARY.md` when done
</output>
