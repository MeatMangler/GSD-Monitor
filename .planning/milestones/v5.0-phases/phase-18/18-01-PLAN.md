---
plan: 18-01
phase: 18
wave: 1
title: "/api/version endpoint + ShellLayout sidebar footer version badge"
status: complete
---

# Plan 18-01: Version Display

## Goal
Expose `gsd_monitor.__version__` via `/api/version` and display it in the ShellLayout sidebar footer.

## Tasks

- [ ] Add `/api/version` endpoint to `gsd_monitor/api/app.py` returning `{"version": __version__}`
- [ ] Add `fetchVersion()` to `frontend/src/api.ts`
- [ ] Add version state + fetch to `ShellLayout.tsx`; render version string in sidebar footer
- [ ] Rebuild frontend
- [ ] Update ROADMAP.md and STATE.md

## Verification

1. `GET /api/version` returns `{"version": "1.0.0"}`
2. Sidebar footer shows `v1.0.0` on every page
3. Bumping `__version__` in `gsd_monitor/__init__.py` updates the displayed string without other code changes
