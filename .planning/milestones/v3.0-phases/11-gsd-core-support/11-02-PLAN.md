---
phase: 11-gsd-core-support
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - frontend/src/api.ts
  - frontend/src/pages/DashboardPage.tsx
  - frontend/src/pages/DocsPage.tsx
  - frontend/src/ShellLayout.tsx
autonomous: false
requirements:
  - DETECT-01
  - DETECT-04
  - DETECT-06
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06
  - DOCS-07
  - DOCS-08
  - PROG-01
  - PROG-02

must_haves:
  truths:
    - "gsd-core projects show 'gsd-core' version badge with bg-[#007acc]/30 styling"
    - "GSD-2 version checks and conditionals are removed from all frontend files"
    - "Dashboard shows progress bar (4px compact on card, 8px detail variant with label)"
    - "Pause banner appears below breadcrumb when handoff_info is present with amber styling"
    - "Config summary badges (workflow mode, model profile, branching strategy) appear below version badge for gsd-core projects"
    - "DocsPage quick-access list includes VERIFICATION, UI-SPEC, UI-REVIEW, SUMMARY shortcuts when present"
    - "Phase cards display p.code when present instead of padded number"
    - "Legacy GSD-1 projects render correctly with 'GSD-1' badge"
    - "No GSD-2 badge or version check exists in frontend code"
  artifacts:
    - path: "frontend/src/api.ts"
      provides: "Updated TypeScript interfaces with new PhasePayload and GsdProjectPayload fields"
      contains: "has_ui_spec"
    - path: "frontend/src/pages/DashboardPage.tsx"
      provides: "Progress bar, pause banner, config badges, version badge, phase ID display"
      contains: "gsd-core"
    - path: "frontend/src/pages/DocsPage.tsx"
      provides: "Quick-access shortcuts for new doc types"
      contains: "UI-SPEC"
    - path: "frontend/src/ShellLayout.tsx"
      provides: "GSD-2 conditional removal"
  key_links:
    - from: "frontend/src/pages/DashboardPage.tsx"
      to: "frontend/src/api.ts"
      via: "uses GsdProjectPayload.handoff_info, config_info, progress_percent fields"
      pattern: "handoff_info|config_info|progress_percent"
    - from: "frontend/src/pages/DocsPage.tsx"
      to: "frontend/src/api.ts"
      via: "uses PhasePayload.has_ui_spec, has_ui_review, has_summary fields"
      pattern: "has_ui_spec|has_summary"
---

<objective>
Implement all frontend changes for gsd-core support: update TypeScript interfaces, add progress bar, pause banner, config badges, version badge update, quick-access doc shortcuts, phase ID display, and GSD-2 frontend cleanup.

Purpose: Users see gsd-core projects fully rendered with progress metrics, pause status, config summary, and all new document types accessible (per D-04, D-06, D-08, D-11, UI-SPEC).

Output: Working frontend displaying gsd-core project data with all UI-SPEC components, verified visually.
</objective>

<execution_context>
@D:/gsd-monitor-py/.claude/gsd-core/workflows/execute-plan.md
@D:/gsd-monitor-py/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-gsd-core-support/11-CONTEXT.md
@.planning/phases/11-gsd-core-support/11-UI-SPEC.md
@.planning/phases/11-gsd-core-support/11-01-SUMMARY.md
</context>

## Artifacts this phase produces

**Modified interfaces in `frontend/src/api.ts`:**
- `PhasePayload` gains: `has_ui_spec: boolean`, `has_ui_review: boolean`, `has_summary: boolean`, `has_requirements: boolean`
- `GsdProjectPayload` gains: `handoff_info?: { phase?: string; plan?: string; timestamp?: string; paused?: boolean } | null`, `continue_here?: boolean`, `config_info?: { workflow_mode?: string; model_profile?: string; branching_strategy?: string } | null`, `progress_percent?: number`, `completed_phases?: number`, `total_phases?: number`

**New UI elements in `DashboardPage.tsx`:**
- Compact progress bar (4px) inside Completion stats card
- Detail progress bar (8px) with "{completed} of {total} phases complete" label
- Pause status banner (amber themed) below breadcrumb
- Config summary badge row below version badge
- Updated version badge: "gsd-core" with bg-[#007acc]/30, "GSD-1" with bg-[#2a2d2e]
- Phase ID display using p.code when present

**New quick-access items in `DocsPage.tsx`:**
- VERIFICATION ({padded}), UI-SPEC ({padded}), UI-REVIEW ({padded}), SUMMARY entries

**Removed from `ShellLayout.tsx`:**
- Any GSD-2 version conditionals (gsdVersion === "gsd2" checks)

<tasks>

<task type="auto">
  <name>Task 1: TypeScript interfaces and DashboardPage updates</name>
  <files>
    frontend/src/api.ts,
    frontend/src/pages/DashboardPage.tsx
  </files>
  <read_first>
    frontend/src/api.ts,
    frontend/src/pages/DashboardPage.tsx,
    .planning/phases/11-gsd-core-support/11-UI-SPEC.md,
    .planning/phases/11-gsd-core-support/11-CONTEXT.md
  </read_first>
  <action>
    UPDATE api.ts INTERFACES: Add to PhasePayload interface: has_ui_spec (boolean), has_ui_review (boolean), has_summary (boolean), has_requirements (boolean). Add to GsdProjectPayload interface: handoff_info (optional object with phase, plan, timestamp strings and paused boolean, or null), continue_here (optional boolean), config_info (optional object with workflow_mode, model_profile, branching_strategy strings, or null), progress_percent (optional number), completed_phases (optional number), total_phases (optional number).

    UPDATE DashboardPage.tsx VERSION BADGE (per DETECT-01, UI-SPEC section 6): Replace the existing ternary that checks gsdVersion === "gsd2". New logic: if stats.gsdVersion equals "gsd-core", render badge text "gsd-core" with classes "rounded bg-[#007acc]/30 px-2 py-0.5 text-xs font-medium text-[#cccccc]". If gsdVersion equals "gsd1", render badge text "GSD-1" with classes "rounded bg-[#2a2d2e] px-2 py-0.5 text-xs font-medium text-[#858585]". No other version values rendered.

    ADD PROGRESS BAR TO DASHBOARD (per PROG-02, D-08, UI-SPEC section 1):
    Compact variant: Inside the Completion stats card (the first card in the grid), below the percentage text and label, add a full-width div with height 4px (h-1), rounded-full, bg-[#474747]. Inside it, a child div with height 4px, rounded-full, bg-[#4ec994], width set via inline style to stats.completion percent. Only render when activeProject has progress data (progress_percent field exists and total_phases > 0).

    Detail variant: Below the stats grid (after mb-6 grid div), add a section showing "X of Y phases complete" as text-xs text-[#858585], followed by a full-width bar at h-2 (8px), rounded-full, bg-[#474747], with fill div bg-[#4ec994] width percentage. Show percentage inline. Only render when progress data available from activeProject (progress_percent, completed_phases, total_phases fields). Use activeProject fields directly, not stats computed values, for the detail variant to get STATE.md-sourced values.

    ADD PAUSE BANNER (per DOCS-06, D-06, UI-SPEC section 2): Between breadcrumb and stats grid, conditionally render a pause banner when activeProject.handoff_info exists and activeProject.handoff_info.paused is true. Banner markup: div with classes "mb-4 rounded-md border border-amber-800/40 bg-amber-900/20 px-4 py-2 flex items-center justify-between". Left side: span with 4px amber-400 circle (inline-block w-1 h-1 rounded-full bg-amber-400 mr-2), then "Paused" text (text-xs font-semibold text-amber-400). Right side: "Phase {handoff_info.phase}, Plan {handoff_info.plan}" followed by " · paused " and formatted date from handoff_info.timestamp, all in text-xs text-[#858585]. Use the fmtDate utility for the timestamp.

    ADD CONFIG BADGE ROW (per DOCS-08, D-07, UI-SPEC section 3): Below the version badge div, conditionally render a row of small badges when activeProject.config_info exists. Render up to three badges for: config_info.workflow_mode, config_info.model_profile, config_info.branching_strategy. Each badge: span with classes "rounded bg-[#2a2d2e] px-1.5 py-0.5 text-xs text-[#858585]". Only render badges for non-null/non-undefined values. Wrap in a div with "mb-4 flex items-center gap-2".

    UPDATE PHASE ID DISPLAY (per DETECT-04, UI-SPEC section 5): In the phase list button and drawer title, replace the hardcoded "String(p.number).padStart(2, '0')" with: if p.code is present and non-empty, use p.code; otherwise use String(p.number).padStart(2, "0"). Apply this in both the phase list button label and the Drawer title prop.
  </action>
  <verify>
    <automated>cd D:/gsd-monitor-py/frontend && npx tsc -b --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - PhasePayload in api.ts has has_ui_spec, has_ui_review, has_summary, has_requirements boolean fields
    - GsdProjectPayload in api.ts has handoff_info, config_info, progress_percent, completed_phases, total_phases fields
    - DashboardPage renders "gsd-core" badge when version equals "gsd-core"
    - DashboardPage renders "GSD-1" badge when version equals "gsd1"
    - DashboardPage does not contain the string "gsd2" or "GSD-2"
    - DashboardPage contains a 4px progress bar inside the Completion card
    - DashboardPage contains an 8px progress bar with "{completed} of {total} phases complete" label
    - DashboardPage contains a pause banner with amber-400, amber-800, amber-900 classes
    - DashboardPage contains config badge rendering checking config_info
    - Phase cards use p.code when present for display
    - TypeScript compilation succeeds with zero errors
    - Vite build succeeds
  </acceptance_criteria>
  <done>DashboardPage shows version badge, progress bars, pause banner, config badges, and phase IDs correctly. TypeScript compiles and builds.</done>
</task>

<task type="auto">
  <name>Task 2: DocsPage quick-access shortcuts and ShellLayout GSD-2 cleanup</name>
  <files>
    frontend/src/pages/DocsPage.tsx,
    frontend/src/ShellLayout.tsx
  </files>
  <read_first>
    frontend/src/pages/DocsPage.tsx,
    frontend/src/ShellLayout.tsx,
    frontend/src/api.ts,
    .planning/phases/11-gsd-core-support/11-UI-SPEC.md
  </read_first>
  <action>
    UPDATE DocsPage QUICK-ACCESS (per DOCS-01 through DOCS-05, D-04, UI-SPEC section 4): Extend the quickAccess useMemo to add shortcuts for new doc types when they exist on the active in-progress phase. After the existing REQUIREMENTS.md entry, conditionally add:
    - If in-progress phase has has_validation === true: add entry with label "VERIFICATION ({padded})" and path "phases/{padded}-{slug}/{padded}-VERIFICATION.md" (construct path from phase artifacts or use the pattern phases/{padded}-*/padded-VERIFICATION.md). Since the exact phase directory slug is unknown to the frontend, use a simpler approach: scan artifact_paths for files ending in "-VERIFICATION.md" and use the relative path from that. For VERIFICATION, the artifact_paths array on PhasePayload already contains absolute paths. Convert to relative paths by stripping the planningPath prefix.
    - If in-progress phase has has_ui_spec === true: add "UI-SPEC ({padded})" using the same artifact_paths approach, finding the file ending in "-UI-SPEC.md"
    - If in-progress phase has has_ui_review === true: add "UI-REVIEW ({padded})" finding "-UI-REVIEW.md"
    - If in-progress phase has has_summary === true: add "SUMMARY ({padded})" finding "-SUMMARY.md"

    The label format follows the existing pattern of "PLAN (05)" per UI-SPEC copywriting contract: "{DOCTYPE} ({padded-phase-number})".

    To convert artifact_paths to relative paths: given activeSegment.planningPath and an artifact absolute path, strip the planningPath prefix and normalize slashes to forward. Use the helper: path.startsWith(root) ? path.slice(root.length).replace(/^[/\\]+/, "").replace(/\\/g, "/") : null. This pattern is already used in DocsPage for activePlanPath.

    SHELLLAYOUT GSD-2 CLEANUP (per D-11): Inspect ShellLayout.tsx for any gsdVersion === "gsd2" conditionals or GSD-2 specific rendering. Currently ShellLayout.tsx does not have explicit GSD-2 checks (confirmed from reading), but verify and remove any if found. The segment selector filters should continue to work for gsd-core and gsd1 segments.

    REBUILD: After changes, rebuild frontend with npm run build in frontend directory.
  </action>
  <verify>
    <automated>cd D:/gsd-monitor-py/frontend && npx tsc -b --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - DocsPage quickAccess includes VERIFICATION entry when in-progress phase has has_validation true
    - DocsPage quickAccess includes UI-SPEC entry when in-progress phase has has_ui_spec true
    - DocsPage quickAccess includes UI-REVIEW entry when in-progress phase has has_ui_review true
    - DocsPage quickAccess includes SUMMARY entry when in-progress phase has has_summary true
    - Quick-access labels follow pattern "{DOCTYPE} ({NN})" using padded phase number
    - ShellLayout.tsx contains no "gsd2" or "GSD-2" string references
    - TypeScript compilation succeeds with zero errors
    - Vite build succeeds and frontend/dist/index.html exists
  </acceptance_criteria>
  <done>DocsPage shows quick-access shortcuts for all new doc types. ShellLayout has no GSD-2 references. Frontend builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of gsd-core UI</name>
  <files>none</files>
  <action>
    Human verifies the full gsd-core UI: version badges, progress bars, pause banner, config badges, phase IDs, quick-access shortcuts, GSD-2 removal.
    Steps:
    1. Start the app: run python -m gsd_monitor from D:/gsd-monitor-py
    2. Ensure scan roots include a gsd-core project (one with .planning/config.json)
    3. Dashboard: verify gsd-core badge (blue tinted), GSD-1 badge (gray), progress bar in Completion card (4px), detail progress bar below stats, pause banner if HANDOFF.json present, config badges if config.json present, milestone-prefixed phase IDs
    4. Docs page: verify quick-access shortcuts for VERIFICATION, UI-SPEC, UI-REVIEW, SUMMARY when present; click to load content
    5. Verify no GSD-2 projects appear
    6. Verify legacy GSD-1 projects still render correctly
  </action>
  <verify>Human visual inspection</verify>
  <done>All gsd-core UI elements render correctly per UI-SPEC. Legacy projects unaffected. No GSD-2 artifacts visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> React state | JSON from backend consumed by frontend components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-F01 | Spoofing | handoff_info rendering | accept | Data comes from local backend reading local files; no external input |
| T-11-F02 | Information Disclosure | config_info badges | accept | Config is non-secret workflow settings; app is local-only |
</threat_model>

<verification>
1. TypeScript compiles: `cd frontend && npx tsc -b --noEmit`
2. Vite builds: `cd frontend && npm run build`
3. Visual verification via human checkpoint
</verification>

<success_criteria>
- All frontend UI-SPEC components rendered correctly
- GSD-2 references fully removed from frontend
- gsd-core projects display with all new UI elements
- Legacy GSD-1 projects continue to render correctly
- Frontend builds with zero TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/11-gsd-core-support/11-02-SUMMARY.md` when done
</output>
