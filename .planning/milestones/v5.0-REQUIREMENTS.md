# Requirements Archive: GSD Monitor v5.0

**Archived:** 2026-07-18
**Status:** ✅ All requirements shipped

---

## v5.0 Installation and Distribution

### Installation (INST)

- [x] **INST-01**: User can run a single PowerShell script on a clean Windows machine (Git + Python 3.11+ + Node.js present) and end up with GSD Monitor fully installed and launchable via a desktop shortcut
- [x] **INST-02**: Installer checks for WebView2 and prints a clear message with a download link if absent
- [x] **INST-03**: Installer exits with a human-readable error naming any missing prerequisite (Git, Python, Node.js)
- [x] **INST-04**: Running the installer a second time on an existing installation completes without corrupting it (idempotent re-run)
- [x] **INST-05**: Installer creates a `.bat` launcher in the install directory so the user can launch without a shortcut

### Upgrade (UPG)

- [x] **UPG-01**: User can run a standalone upgrade script from the install directory that performs: `git pull`, `pip install -e .`, `npm ci && npm run build` in `frontend/`
- [x] **UPG-02**: Upgrade script exits with a human-readable error if run from outside a valid GSD Monitor installation
- [x] **UPG-03**: Upgrade script completes cleanly when already on the latest version (no error, clear confirmation message)

### Version Display (VER)

- [x] **VER-01**: The ShellLayout sidebar footer displays GSD Monitor's version number, read from `gsd_monitor/__init__.py` via the API

---

## Traceability

| Requirement | Phase | Status | Outcome |
|-------------|-------|--------|---------|
| INST-01 | Phase 17 | ✅ Validated | `install.ps1` end-to-end: clone → venv → pip install -e . → npm build → start.bat → desktop shortcut |
| INST-02 | Phase 17 | ✅ Validated | Registry check (HKLM + HKCU) with `Fail` message linking to Microsoft download page |
| INST-03 | Phase 17 | ✅ Validated | Separate `Fail` call per prereq with name and download link |
| INST-04 | Phase 17 | ✅ Validated | `.git` detection → upgrade branch shown; interactive Y/n prompt; no re-clone |
| INST-05 | Phase 17 | ✅ Validated | `start.bat` written to install dir; same path used as shortcut target |
| UPG-01 | Phase 17 | ✅ Validated | `upgrade.ps1`: git pull → pip install -e . → npm ci → npm run build |
| UPG-02 | Phase 17 | ✅ Validated | Guard checks `gsd_monitor/` dir + `.venv/Scripts/pip.exe`; exits with message if absent |
| UPG-03 | Phase 17 | ✅ Validated | Hash comparison (current vs origin/master); exits 0 with "Already up to date" if equal |
| VER-01 | Phase 18 | ✅ Validated | `/api/version` → `ShellLayout` footer `fetchVersion()` on mount; visible on all pages |

---

## Out of Scope (Confirmed)

- Chocolatey / winget package — formal Windows package manager distribution
- Auto-update check — background version check against GitHub releases
- Uninstall script — clean removal of launcher, shortcut, venv
- In-app upgrade trigger — upgrade is scripted-only (no UI button)
- macOS/Linux installers — Windows-only platform constraint
- gsd-core project version display — only GSD Monitor's own version shown
- Signed executables / code signing — distribution complexity out of scope for v5.0

---

## Notes

- All requirements verified via milestone audit (`.planning/milestones/v5.0-MILESTONE-AUDIT.md`) — audit passed
- REQUIREMENTS.md checkboxes were `[ ]` at time of archival (documentation gap only; all requirements confirmed implemented in audit)
- Detailed manual verification steps documented in `REQUIREMENTS-VERIFICATION.md` (project root)
