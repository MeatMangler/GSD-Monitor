# Milestone Audit: v5.0 — Installation and Distribution

**Audited:** 2026-07-18
**Milestone:** v5.0
**Phases:** 17 (Installation & Upgrade Scripts), 18 (Version Display)
**Plans:** 3/3 complete
**Verdict:** ✅ PASSED — all requirements satisfied

---

## Requirements Coverage

| Req | Description | Status | Evidence |
|-----|-------------|--------|----------|
| INST-01 | Single PS script → working install + desktop shortcut | ✅ | `install.ps1` exists; sections 1–6 cover clone→venv→deps→build→bat→shortcut; commit d69ec64 |
| INST-02 | WebView2 check with download link if absent | ✅ | `install.ps1:102-119` — checks HKLM/HKCU registry; `Fail` prints download URL |
| INST-03 | Human-readable error for missing prerequisite | ✅ | `install.ps1:70-100` — separate `Fail` calls for Git, Python, Node with download links |
| INST-04 | Idempotent re-run (no corruption) | ✅ | `install.ps1:153` — detects `.git` presence, shows current/latest hash, offers upgrade path |
| INST-05 | `.bat` launcher in install directory | ✅ | `install.ps1:223-295` — writes `start.bat` template; also creates desktop shortcut |
| UPG-01 | Upgrade: `git pull` + `pip install -e .` + `npm ci && npm run build` | ✅ | `upgrade.ps1` lines 88-120; `upgrade.bat` 2-line launcher; commit 69f06c6 |
| UPG-02 | Error if run outside valid GSD Monitor directory | ✅ | `upgrade.ps1:53-59` — guards on `gsd_monitor/` folder + `.venv/Scripts/pip.exe` |
| UPG-03 | Clean exit when already on latest version | ✅ | `upgrade.ps1:~78-80` — hash comparison exits 0 with "Already up to date" message |
| VER-01 | Sidebar footer shows version from `gsd_monitor.__version__` via API | ✅ | `/api/version` endpoint at `app.py:380-383`; `ShellLayout.tsx:37-39,264-269`; commit 1d76f6b |

---

## Phase Artifacts Audit

### Phase 17 — Installation & Upgrade Scripts

| Artifact | Status |
|----------|--------|
| 17-01-PLAN.md | ✅ present |
| 17-01-SUMMARY.md | ✅ present (pyproject.toml + install.ps1 pip -e) |
| 17-02-PLAN.md | ✅ present |
| 17-02-SUMMARY.md | ✅ present (upgrade.ps1 + upgrade.bat) |
| 17-VERIFICATION.md | ❌ not created |

### Phase 18 — Version Display

| Artifact | Status |
|----------|--------|
| 18-* directory | ❌ no phase directory created |
| 18-01-PLAN.md | ❌ not created |
| 18-01-SUMMARY.md | ❌ not created |
| 18-VERIFICATION.md | ❌ not created |

Phase 18 was executed inline (no phase directory under `.planning/phases/`). Deliverables are confirmed in the codebase and the ROADMAP records it as complete with commit 1d76f6b.

---

## Cross-Phase Integration

- **Version source chain:** `gsd_monitor/__init__.py:__version__ = "1.0.0"` → `pyproject.toml` dynamic hatch version → `/api/version` endpoint → `fetchVersion()` in `api.ts` → `ShellLayout` footer state — end-to-end wiring confirmed.
- **Upgrade vs install paths:** `install.ps1` re-run uses `git reset --hard origin/master` (intentional for installer context; `upgrade.ps1` uses `git pull` per UPG-01 spec). Both paths rebuild frontend.
- **upgrade.ps1 uses `npm ci`** (not `npm install`) — matches UPG-01 specification exactly.

---

## Observations / Tech Debt

1. **REQUIREMENTS.md checkboxes unchecked** — all 9 requirements still show `[ ]` rather than `[x]`. Should be updated at milestone close.
2. **Phase 18 has no planning artifacts** — executed without a phase directory. Consistent with "fast track" execution but leaves a traceability gap. ROADMAP entry is the only record.
3. **Phase 17 missing VERIFICATION.md** — no verification file produced for Phase 17. Success criteria were verified via SUMMARY self-checks.
4. **install.ps1 re-run is interactive** — uses `Read-Host` to ask "Upgrade? [Y/n]". INST-04 says "completes without error" which it does, but it requires keyboard input. Acceptable per spec language but worth noting for future automation scenarios.

---

## Verdict

**✅ PASS** — v5.0 definition of done is satisfied. All 9 requirements (INST-01–05, UPG-01–03, VER-01) are implemented and confirmed in the codebase. Three commits land the work cleanly. Minor planning artifact gaps (no Phase 18 directory, missing verification files) are documentation-only gaps — no functional regression.

**Recommended before archiving:**
1. Mark all `[ ]` checkboxes in REQUIREMENTS.md as `[x]`
2. Update MILESTONES.md with v5.0 shipped entry
3. Optionally create a minimal `18-version-display/18-01-SUMMARY.md` for traceability
